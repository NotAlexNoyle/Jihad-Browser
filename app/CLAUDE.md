# Source Code — UI shell (Enyo, Apache-2.0)

Forked from isis-browser. Talks to the render daemon via `callBrowserAdapter`
and `PalmServiceBridge`. Keep the YAP/BrowserAdapter contract unchanged.

See context/kits/ for requirements this code implements.
See context/plans/ for task dependency graphs.
