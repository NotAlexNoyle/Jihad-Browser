---
created: "2026-08-05"
last_edited: "2026-08-05"
---

# Cavekit: GRE Widget Bindings

## Scope
The XBL widget bindings UXP already ships in our dist, and whether they actually work here.
**49 binding files** are installed at `dist/bin/chrome/toolkit/content/global/bindings/`, wired
up in two places that this domain keeps carefully apart:

- **`xul.css` wires most of them for CHROME documents** — any page we author gets them free.
- **`html.css` and `forms.css` wire four for CONTENT** — they apply to every website, whether or
  not we ever thought about them: `videocontrols.xml`, `datetimebox.xml`
  (`date-input`/`time-input`), and `platformHTMLBindings.xml` (`inputFields`/`textAreas`).

Full inventory, including the ones deliberately not used: `../impl/impl-gre-widget-inventory.md`.

**Why this is a domain and not a footnote.** The content-facing four are user-facing web-platform
behaviour that NO existing requirement mentions — a website with a `<video>` or an
`<input type="date">` exercises engine paths this project has never tested. And the chrome-facing
ones are free implementation we keep not using: the preferences page in cavekit-preferences-ui.md
is the immediate consumer, but so is every future about: page.

**User requirement, 2026-08-05:** *"bring in all useful widgets as goals."*

## Requirements

### R1: HTML5 media elements work, or the limit is documented
**Description:** `<video>`/`<audio>` get the GRE's own controls, and playback either works or its failure is recorded precisely.
**Acceptance Criteria:**
- [x] The `videocontrols.xml#videoControls` binding ATTACHES to a `<video>` in content — verified by reaching its anonymous content, not by the `controls` DOM attribute (which reflects regardless and proves nothing). *(**2026-08-10 (T-125) — ATTEMPTED ON DEVICE AND THE PROBE ROUTE IS CLOSED. Recorded so the next person does not spend the run finding out.** A `<video controls src=jt.webm>` page was loaded and probed through the inject channel's `jsurl`, which runs with the SYSTEM principal (`DebugRunChromeJs`, `GoannaRenderPage.cpp:553`) — so privilege was NOT the obstacle. The result was `EXC TypeError: document.getAnonymousNodes is not a function`. That API lives on `nsIDOMDocumentXBL` and is simply not reachable from JS here, system principal or not. The same run DID confirm the element itself is live: `readyState=4`, `currentTime` advancing to 1.13 s, i.e. VP8 plays with `controls` present — but that is exactly the evidence this criterion says proves nothing.*
  ***So the NAC walk has to happen in C++***, *the way the earlier datetimebox probe concluded: element → primary frame → native-anonymous child → query `anonid`. That is a small addition to the debug surface (a new inject command next to `rect`/`gettext`), not a page-side trick, and it would answer this criterion and the `datetimebox` one in the same run. Do not retry from page JS.)*
  *(**2026-08-10, LATER — an `anon <selector>` inject command was built for this, and it took THREE
  iterations to become a valid instrument. The two dead ends are recorded because each produced a
  confident, wrong reading.***
  ***Iteration 1 returned `0`***, *which conflated "the accessor failed" with "the element bound
  nothing" — two states needing opposite follow-ups.*
  ***Iteration 2 distinguished them and returned `(null list)`***, *i.e. `GetAnonymousNodes`
  SUCCEEDED and handed back null. That looks like a decisive negative and **it is not one.**
  `GetAnonymousNodes` returns only XBL-bound anonymous content, and `<video controls>` controls
  are not that: `nsVideoFrame` creates a `<xul:videocontrols>` as **native** anonymous content and
  the binding attaches to THAT child, so asking the `<video>` for XBL anonymous nodes returns null
  BY CONSTRUCTION whether or not the controls exist. Corroboration that the negative would have
  been wrong: `videocontrols.xml` is demonstrably shipped — 94 KB in each variant's
  `chrome/toolkit/content/global/bindings/` on the device.*
  ***Iteration 3 uses `getAnonymousElementByAttribute(el, "anonid", "controlBar")`***, *the
  accessor Gecko's own videocontrols tests use, which DOES reach the native anonymous subtree.
  `controlBar` is the anonid `videocontrols.xml` gives its control strip, so finding it is
  positive proof the binding bound. `anon` now reports both halves — `NAC:controlBar
  found|absent | xbl=…` — so neither can be misread as the other again.*
  ***THE ANSWER, with the right instrument: `NAC:controlBar absent`.*** *Clean-restart run,
  `inject: no page` 0, on a `<video controls>` that is demonstrably PLAYING (VP8, `readyState=4`,
  `currentTime` advancing — see the codec criterion). Both selectors agree:
  `anon video` and `anon #v` = `NAC:controlBar absent | xbl=(null list — expected for
  frame-created NAC)`. **So `videocontrols.xml#videoControls` does NOT attach in this build.**
  It is not a missing-file problem: the binding is shipped, 94 KB, in every variant's
  `chrome/toolkit/content/global/bindings/`.*
  ***AND THEN A SCREENSHOT OVERTURNED THAT CONCLUSION — the binding DOES attach.*** *Taken minutes
  later on the same page (`takeScreenShot` → `/tmp/jihad-vctl.png`, 1024x768), the video area shows
  a large ✕ status icon and the text **"Video can't be played because the file is corrupt."**
  That string is `videocontrols.xml`'s OWN `errorDecode` label, drawn by its status overlay
  (`videocontrols.xml:612-632` handles `error`, `updateErrorText()` at `:694-726` picks the label
  off `video.error.code`). **A binding that did not attach cannot render its own error UI.**
  So `NAC:controlBar absent` was a true reading of a misleading question: on the error path
  videocontrols calls `startFadeOut(this.controlBar)`, so the control BAR is gone while the
  binding is very much alive. Querying one anonid and generalising from its absence was the
  mistake — the fifth confident inference overturned by measurement today, and the only one caught
  by looking at pixels rather than by a better API call.*
  ***Marking this criterion `[x]`:*** *it asks whether the binding ATTACHES, verified by reaching
  its anonymous content rather than by the `controls` attribute. The rendered overlay is stronger
  evidence than the anonid query — it is the binding's own output.*
  ***~~When re-testing, use a video that does NOT error, or query `anonid="statusOverlay"`
  instead of `controlBar`.~~ THAT RETEST ADVICE IS WRONG AND WOULD HAVE COST ANOTHER DEVICE RUN
  — corrected 2026-08-10 (T-126), from source.*** *Neither half of it works, and the reason is
  that `NAC:controlBar absent` was never a reading of the engine at all. The query could not have
  returned non-null on ANY video, erroring or not, for two independent reasons:*
  1. ***The accessor is HOST-SCOPED, not a subtree search.*** *`nsDocument::
     GetAnonymousElementByAttribute` goes straight to `BindingManager()->GetAnonymousNodesFor`
     (`dom/base/nsDocument.cpp:5848`), which is
     `GetBindingWithContent(elem)->GetAnonymousNodeList()` (`dom/xbl/nsBindingManager.cpp:250-254`)
     — the XBL binding on the element you PASS. A `<video>` never has one. `videocontrols.xml`
     itself shows the intended call: its `init()` does
     `getAnonymousElementByAttribute(binding, "class", …)` with `binding` = the
     `<xul:videocontrols>` element (`:1540-1553`). So iteration 3 bottomed out in the SAME
     function as iteration 2, and "both selectors agree" was one broken accessor asked twice.*
  2. ***There is no `anonid="controlBar"` in this UXP at all — nor `anonid="statusOverlay"`.***
     *The strip is `<hbox class="controlBar">` (`:225`) and the overlay is
     `<vbox class="statusOverlay">` (`:209`); the ONLY `anonid`s in that 94 KB file are the six
     `error*` labels and `volumeForeground`. The binding finds every one of its own parts by
     CLASS.*
  *The stated mechanism above — "on the error path videocontrols calls `startFadeOut(controlBar)`,
  so the control BAR is gone" — is also wrong as an explanation of the null, and worth correcting
  because it sounds right. `startFade` sets a `fadeout` ATTRIBUTE (`:956-985`, the write at `:979`); the skin turns
  that into `opacity: 0` (`toolkit/themes/shared/media/videocontrols.css:352-358`) and
  `onTransitionEnd` sets `hidden` (`:987-1000`). The node stays in the anonymous tree throughout,
  so a working accessor would still have found it.*
  ***The instrument is now fixed rather than documented around*** *(`render/goanna/
  GoannaRenderPage.cpp`, `DebugAnonNodes`): `anon` walks NAC through
  `inIDOMUtils::getChildrenForNode(node, true)` = `nsIContent::GetChildren(eAllChildren)`
  (`layout/inspector/inDOMUtils.cpp:192-214`, `dom/base/nsIContent.h:117-128`), and reports
  `NAC:<n> <name>[<k>]…` where `k` is each NAC child's own flattened-children count — non-zero
  ONLY if a binding attached to that child. Cross-compiled clean with the real ARM toolchain and
  the real daemon flags; NOT run, no device this session.)*
  ***Corroborating observable that needs no NAC at all:*** *this requirement's SECOND criterion —
  are the controls visible in a captured frame — is answerable with `takeScreenShot` on the same
  page, and if the controls are absent from the pixels while `controls` is set, that is
  independent evidence for the same conclusion. Do that in the same run.)*
- [~] The controls render into the offscreen surface and are visible in a captured frame. *(**DOWNGRADED `[x]`→`[~]` 2026-08-15 on a USER REPORT — they render, but the SCRUBBER does not animate smoothly.** The user, watching an engine `<audio>` play on device, reported the seek bar is "jittery" and "at the end of a clip it doesn't come to rest." Investigated: the media ELEMENT logic is correct — at clip end the daemon reads `t=3.000 dur=3.00 paused=true ended=true`, i.e. it stops exactly at the duration, fires `ended`, and comes to rest in the DOM. So this is NOT a playback defect; it is a videocontrols SCRUBBER RENDERING issue on the offscreen surface: the thumb's timer-driven repaints composite unevenly (jitter), and the final ended-state repaint does not visually settle the thumb. Same offscreen-composite domain as the Flash frame-pacing work (cavekit-addons-extensions.md R7, cavekit-offscreen-rendering.md) — the controls are chrome-animated content whose repaints ride the daemon's paint tick, and an uneven tick shows as an uneven scrubber. Needs rendering investigation (the scrubber's repaint cadence vs the composite tick); the "renders and is visible in a frame" part is still true (screenshot `/tmp/jihad-vctl.png` earlier), which is why this is `[~]` not struck. Recorded in `../impl/impl-device-2026-08-15.md`.)* *(**MET 2026-08-10 — device screenshot, `/tmp/jihad-vctl.png`.** The videocontrols status overlay is plainly visible in the captured frame: a ✕ icon and the localized string "Video can't be played because the file is corrupt." over the video box. It is the ERROR overlay rather than the play/pause bar — see the note on the criterion above for why the control bar is faded out in that state — but the criterion is about the controls reaching the offscreen surface at all, and they demonstrably do. A non-erroring clip would show the bar itself.)*
- [~] Play/pause is operable by tap through the normal input path, and the control's state changes in response. *(**2026-08-15 — device-tested; `[ ]`→`[~]`, play-direction DEMONSTRATED. Record in `../impl/impl-device-2026-08-15.md`.** On a `<video controls loop>` of a VP8 clip, with the video stopped (`paused=true t=0.00 rs=0`) and its control bar visible, an injected click at the play/pause button location (`clickat 16 232`, bottom-left of the 320×240 element) STARTED playback: the next read was `paused=false t=0.73 rs=4` — the control responded to the tap and the state changed, which is exactly what this criterion asks. **Two honest residuals keep it `[~]`, neither being "the control does not work":** (1) `clickat` is the daemon-level click, one layer below a real finger through the adapter's touch path — the same distinction that keeps the prefs hit-test at `[~]` (T-150, human); (2) the clean PAUSE-direction toggle could not be shown because videocontrols AUTO-HIDES the bar during play (`dynamicControls`), so a second tap lands on the video area not the button, and this build's VP8 decode also STALLS at ~0.73 s (the `err=3` MEDIA_ERR_DECODE this kit already records for VP8) — both confounds, not control failures. A real finger + a non-stalling clip closes it.)*
- [x] **Codec reality is measured and written down, whichever way it goes.** Which containers/codecs this ARM build can actually decode is unrecorded today and is upstream of every other media question — a build with no usable decoder makes the controls cosmetic. State what plays, what does not, and why (missing decoder vs missing hardware path). *(**2026-08-10 — MET. Measured in two halves the same day: the build config, then the device.** The build half had been sitting in `mozconfig.goanna-arm` and the objdir the whole time and nobody had read it; the device half then corrected what the build half predicted.*
  - ***DECODERS ARE PRESENT.*** *`autoconf.mk` carries `MOZ_FFVPX=1`, `MOZ_FFMPEG=1`, `MOZ_FMP4=1`, `MOZ_WEBM_ENCODER=1`, `MOZ_SAMPLE_TYPE_S16=1`, and 110 object files are compiled under `media/ffvpx` — so VP8/VP9/Theora/MP3/AAC software decode is built in. There is no separate `libmozavcodec.so`/`libmozavutil.so` in `dist/`; it is linked into `libxul.so`, and `libxul.so` is the only engine binary shipped to the device.*
  - ***THERE IS NO AUDIO OUTPUT AT ALL.*** *`mozconfig.goanna-arm` passes `--disable-alsa` AND `--disable-pulseaudio` (lines 68-69), and the proof is in the objdir rather than the flags: `media/libcubeb/src/` compiled exactly two objects, `cubeb.o` and `cubeb_panner.o` — **no backend**. cubeb has nothing to open a device with, so `<audio>` and `<video>` decode into silence. This is a missing OUTPUT path, not a missing decoder, which is precisely the distinction this criterion asks for.*
  - ***This does NOT contradict "Flash produces sound"*** *(cavekit-addons-extensions.md R7). Flash does not use the engine's audio path at all — it `dlopen`s `libasound` itself from inside plugin-container. That is why the plugin has sound while engine media would not, and it is why the two must never be used as evidence about each other.*
  - ***MEASURED ON DEVICE, same day — and it corrected the prediction above.*** *`/tmp/jihad-media.html` (a `<video src=jt.webm>` + `<audio src=jt.mp3>` reporting `canPlayType`/`readyState`/`currentTime`/`error`), read back with `gettext #r` on a clean-restart run:*
    *`video canPlay webm/vp8: probably | video canPlay mp4/h264: no | audio canPlay mpeg: no | video readyState=4 t=1.40 w=160x120 err=3 | audio readyState=0 t=0.00 err=4`*
    - ***VP8/WebM video STARTS, then FAILS.*** *`readyState=4` (HAVE_ENOUGH_DATA), correct `160x120` intrinsic size, and `currentTime` advancing to 1.13-1.40 s — so the ffvpx software decoder genuinely runs on this hardware and produces frames. **But it does not survive:** `err=3` (MEDIA_ERR_DECODE) is set, and a device SCREENSHOT taken later the same day shows the user-visible consequence — videocontrols' error overlay with a ✕ and "Video can't be played because the file is corrupt." over the video box.*
      ***CORRECTION, and the wrong version is left visible because the mistake is the lesson:*** *an earlier revision of this bullet read "VP8/WebM video PLAYS" and dismissed `err=3` as "a loop/teardown artefact to re-check, not as video is broken". That was an inference from a number, and the pixels refuted it. Playback starting is not playback working.*
      ***WHAT IS NOT YET KNOWN: whether the fault is the DECODER or the ASSET.*** *`jt.webm` was generated with `ffmpeg -f lavfi -i testsrc -c:v libvpx` and has never been validated. **This project has already lost a session to exactly this** — every "Flash audio is broken" measurement was taken against SWFs with a duplicate character ID that no player could have played (see cavekit-addons-extensions.md R7). Before concluding anything about ffvpx: play the same file in a desktop build, and try a second clip from an independent encoder.*
    - ***H.264/MP4: NOT supported*** *— `canPlayType` returns empty despite `MOZ_FMP4=1`, so the FMP4 path has no usable platform decoder here.*
    - ***MP3 audio: REFUSED OUTRIGHT, not silent.*** *`canPlayType('audio/mpeg')` is empty and the element ends at `readyState=0` with `error.code=4` (MEDIA_ERR_SRC_NOT_SUPPORTED). **The prediction one bullet above — "decode into silence" — was WRONG**, and is left in place rather than quietly edited because it is the third time this session that a confident inference about this pipeline did not survive being measured. ~~With no cubeb backend the engine does not offer the type at all.~~ **THAT CAUSE IS ALSO WRONG — corrected 2026-08-10 (T-138), from source.** `canPlayType` never consults the audio OUTPUT; the absent cubeb backend is a separate defect. MP3 is refused for lack of a **DECODER**: `MP3Decoder::CanHandleMediaType` asks `PDMFactory::SupportsMimeType("audio/mpeg")` (`dom/media/mp3/MP3Decoder.cpp:31-34`), and ffvpx is compiled with `CONFIG_MP3_DECODER 0` — all six MP3 variants 0, `third_party/uxp/media/ffvpx/config_components_audio_video.h:386-391`, with nothing overriding them in `config_override.h` — while the device carries no system `libavcodec` for `FFmpegRuntimeLinker` to fall back to. Same story for H.264: `CONFIG_H264_DECODER 0` (`:127`) and `CONFIG_AAC_DECODER 0` (`:330`). **This matters because MP3's clean refusal does NOT generalise:** ffvpx DOES build VP8, VP9 and FLAC (`:299`, `:302`, `:367`), and `AgnosticDecoderModule` supplies Vorbis, Opus, Wave and Theora — so `audio/ogg`, `audio/webm`, `audio/wav` and `audio/flac` ARE offered and then decode into a nonexistent output, which is the opposite failure mode. See this requirement's last criterion.*
  - ***Bottom line for anyone planning media work:*** *WebM/VP8 is the only format that plays, and nothing the ENGINE plays can make sound. `--disable-webrtc` is also set, so capture is out of scope regardless.)*
- [~] If media is not viable on this device, `<video>` degrades visibly rather than silently: the element must not render as a permanent blank box with working-looking controls. *(**T-138, 2026-08-10 — RESOLVED AT THE PREF LEVEL, SOURCE-READ ONLY, no device. `<video>` STAYS ON.** The antecedent is false for video: VP8/WebM plays (criterion above), so a blanket disable would be wrong. The decision and its reason are recorded in `packaging/prefs/jihad-platform-prefs.js` where R7 requires them, and `media.webm.enabled` is pinned there — already the default, pinned because it is the only working media path and a trimming pass must not take it silently.*
  - ***For an unsupported VIDEO track the GRE already degrades visibly and we do not have to build anything.*** *`videocontrols.xml`'s `error` branch (`:612-632`) sets `statusIcon type="error"`, picks the label off `video.error.code` in `updateErrorText` (`:694-726`) — `MEDIA_ERR_SRC_NOT_SUPPORTED` renders "Video format or MIME type is not supported." — shows the status overlay, and fades the control bar OUT when no frame was ever shown. Everything it needs is in `build/webos-oe/device-bundle/`: `videocontrols.xml`, `chrome/en-US/locale/en-US/global/videocontrols.dtd` (the `error.*` entities are present, so the anonymous content parses at all), `chrome/toolkit/skin/classic/global/media/videocontrols.css` and `error.png`. So an H.264 `<video>` gets a broken-media state, not a blank box.*
  - ***WHY THIS IS `[~]` AND NOT `[x]`: the audio-only branch is exactly the forbidden shape, and it is reachable from a `<video>` ELEMENT — so it is this criterion's own failure mode, not a neighbouring one.*** *`videocontrols` suppresses the error overlay for audio-only content BY CONSTRUCTION: `setupStatusFader` forces `show = false` when `isAudioOnly` (`:336-339`, "until bug 619421 is fixed"), and `loadedmetadata` sets `isAudioOnly = true` for any `<video>` whose `videoWidth`/`videoHeight` is 0 (`:524-531`), after which `dynamicControls` (`:451-461`) is false and `setupNewLoadState` (`:431-442`) force-SHOWS the control bar. A `<video src>` pointing at an audio-only Ogg/WebM therefore renders a permanent blank box with a live-looking control bar and NO error text. And no engine audio can ever play: `cubeb_init`'s backend dispatch table is EMPTY with both backends configured out (`media/libcubeb/src/cubeb.c:114`), so `AudioStream::Init` returns `CUBEB_INITIALIZATION_ERR` (`dom/media/AudioStream.cpp:337-341`).*
  - ***The failure is silent by DESIGN, not by accident.*** *`MediaDecoderStateMachine::OnMediaSinkAudioError` (`:3001-3022`) branches on `HasVideo()`: with video it `return`s — "Make the best effort to continue playback when there is video" — so a WebM carrying a Vorbis track plays picture-only and reports NO error at all; audio-only falls through to `DecodeError` → `HTMLMediaElement::DecodeError` (`:4520-4546`) → `MEDIA_ERR_DECODE`, but only AFTER the user presses play, and the overlay is suppressed anyway.*
  - ***THE PREF LEVER DOES NOT EXIST, and that is the finding.*** *The codec prefs are CONTAINER-scoped, not track-scoped. `media.wave.enabled` and `media.flac.enabled` could be turned off (audio-only containers), but `media.ogg.enabled` also carries Theora video and `media.webm.enabled` carries the VP8 that works — and `audio/ogg` / `audio/webm` are offered with no output check whatsoever (`dom/media/ogg/OggDecoder.cpp:27-30` and `dom/media/webm/WebMDecoder.cpp:29-32` read only the pref; `dom/media/platforms/agnostic/AgnosticDecoderModule.cpp:26-46` advertises Vorbis/Opus/Wave/Theora/VPX regardless of output). Flipping the two that CAN go would leave `audio/wav` refused and `audio/ogg` accepted without closing anything, so NOTHING was flipped. Ruled out, not overlooked.*
  - ***THE FIX IS ONE BUILD FLAG, not a pref.*** *`build/webos-oe/mozconfig.goanna-arm:68` passes `--disable-alsa`, and its stated reason — "Headless render daemon: no audio output needed (offscreen render only)" — is **STALE**: offscreen rendering has nothing to do with audio output, and the daemon is a browser engine expected to play media. cubeb ships an ALSA backend in-tree (`media/libcubeb/src/cubeb_alsa.c`) and ALSA demonstrably works on this device (`render/goanna/test/alsa_probe.c` is audible under BOTH loaders; Flash `dlopen`s libasound from plugin-container and makes sound). Build that backend and this criterion closes without a single pref change. Not done here: it needs an `alsa-lib` header set in the sysroot and a device run.*
  - ***THE BUILD FLAG IS FLIPPED — 2026-08-15. The backend is BUILT; it has NOT been HEARD, and the difference is the whole remaining gap.*** *`mozconfig.goanna-arm` now passes `--enable-alsa` (explicitly, not merely dropping `--disable-alsa`, so an upstream default change cannot silently take it away) with the real reason written next to it. `libasound2` + `libasound2-dev` 1.0.28-1 armel were staged into the cross sysroot from Debian Jessie by the same deb-unpack mechanism as the GTK/cairo/pango libs. Configure reports `checking for alsa... yes`, `autoconf.mk` carries `MOZ_ALSA = 1`, and `media/libcubeb/src/` — which had compiled exactly `cubeb.o` + `cubeb_panner.o` since this port began — now compiles **`cubeb_alsa.o`**, so `cubeb_init`'s dispatch table has an entry and `AudioStream::Init` has something to open. **No pref was changed, exactly as this bullet predicted.** Full record, including the sysroot staging and the runtime-library analysis: `../impl/impl-audio-backend.md`.*
    - ***What this does NOT close, and why the criterion stays `[~]`:*** *nobody has played a sound through the engine on hardware — no device was connected that session. "The output path exists in the binary" is not "audio works", and this file has already been wrong twice about this pipeline by reasoning from a build fact to a device outcome (the "decode into silence" prediction on the criterion above, and "VP8/WebM video PLAYS"). One device run closes it: play an `audio/ogg` or `audio/wav` from the existing `jihad-media.html` harness and listen.*
    - ***A NEW RISK THIS CHANGE INTRODUCES, which must be checked FIRST on that run.*** *`cubeb_alsa.c` contains no `dlopen` — it calls `snd_pcm_open` directly, so `libxul.so` now carries a **hard `DT_NEEDED` on `libasound.so.2`**. An unresolvable libasound no longer degrades to silence; it stops the engine loading at all. Check the daemon STARTS before checking whether it sings.*
    - ***And a specific, predicted failure that would NOT mean the backend is wrong.*** *`cubeb_alsa.c:24` hardcodes the PCM name `"default"`, and on this device `default` routes through the pulse plugin (`docs/PICKUP.md:66` — Flash's libasound opens `/usr/lib/alsa-lib/libasound_module_pcm_pulse.so`). The bundled Jessie libasound's compiled-in plugin directory is `/usr/lib/arm-linux-gnueabi/alsa-lib`, which does not exist on the TouchPad, and this alsa-lib build has no `ALSA_PLUGIN_DIR` override (only `ALSA_CONFIG_PATH`). So the first attempt may be silent for a reason that has nothing to do with cubeb. The two fallbacks — point `default` at plain `hw` via `ALSA_CONFIG_PATH`, or drop the bundled libasound so the device's own (already measured audible under our loader) resolves instead — are written out with their ABI trade-offs in the impl note. Do not re-derive them at the device.*
    - ***A DEPLOY TRAP THIS CREATES, worth knowing before the next device session.*** *`push-engine-update.sh` pushes exactly three files — `libxul.so`, `jihad-browserserver`, `goanna.js` — and no `.so` dependencies, while `make-device-bundle.sh`'s closure walk now copies the Jessie `libasound.so.2` into the bundle automatically. **So the fast dev loop and a full bundle bind DIFFERENT libasounds** (the device's own vs the bundled Jessie one), with different failure modes. Two consequences: the first deploy of this change should be a full bundle, because a dev-loop push leaves the new hard `DT_NEEDED` to be satisfied from the device and a miss means the daemon does not start at all; and any audio result MUST be recorded together with the deploy path that produced it, or it cannot be interpreted.*
    - ***Unchanged by this: the DECODERS.*** *`CONFIG_MP3_DECODER 0`, `CONFIG_AAC_DECODER 0`, `CONFIG_H264_DECODER 0` are untouched and were deliberately not flipped. `audio/mpeg` and H.264 stay refused at `canPlayType` however well the output path works. The backend helps exactly the formats that already decoded into nothing — `audio/ogg`, `audio/webm`, `audio/wav`, `audio/flac`.*
  - ***To close to `[x]`, one device run:*** *(a) an H.264 `<video>` — expect the error overlay and the control bar withdrawn; (b) a `<video>` pointed at an audio-only Ogg/WebM — expect the blank box with a live control bar, which is the bug. Both are `gettext`/screenshot-observable on the existing `jihad-media.html` harness.)*
  - ***T-126, 2026-08-10 — RE-READ AGAINST THE "videocontrols does not attach" SCARE. Everything above SURVIVES; the scare was a broken instrument, not a finding, and this criterion is unchanged by it. Source-read only, no device.***
    - ***The premise T-126 was handed — "no controls attach, so the failure mode is a video area with NO controls at all" — is FALSE, and would have sent this criterion the wrong way entirely.*** *`NAC:controlBar absent` could not have read otherwise on any `<video>` in any build: the accessor is host-scoped and there is no `anonid="controlBar"` in this UXP. Full mechanism on this requirement's FIRST criterion, which also carries the fix to the `anon` command. The screenshot (`/tmp/jihad-vctl.png`, videocontrols' own `errorDecode` string) had already settled attachment before this task ran.*
    - ***So the degradation story is the one T-138 wrote, and it splits by ELEMENT, not by codec.*** *Stated per case because "what does a user see" was the question asked:*
      - ***(a) A playing VP8/WebM `<video controls>`: normal.*** *Picture renders, bar shows on load and auto-fades during play (`dynamicControls` true). **But there is a SECOND instance of "working-looking controls" here that nobody had written down, and it is this criterion's own forbidden shape in the audio dimension.** The mute button and volume slider are disabled only on `!video.mozHasAudio` (`videocontrols.xml:383`, `:533-536`), and `HTMLVideoElement::MozHasAudio` is just `HasAudio()` — the DEMUXED track list (`dom/html/HTMLVideoElement.cpp:198-202`), which knows nothing about output. A WebM carrying a Vorbis/Opus track therefore shows an ENABLED, draggable volume control that can never make a sound, and reports no error at all, because `OnMediaSinkAudioError` returns early whenever `HasVideo()`. **Our own asset cannot show this** — the `jihad-media.html` recipe in `docs/PICKUP.md` builds `jt.webm` from `testsrc -c:v libvpx`, i.e. video-only, so `mozHasAudio` is false and the mute button is correctly disabled. Reproducing it needs a WebM WITH an audio track, which no asset in this tree has.*
      - ***(b) An unsupported H.264 `<video controls>`: degrades visibly. Closed, and now corroborated on hardware rather than only read.*** *The screenshot on the first criterion is the same overlay machinery firing end to end — status icon, localized label, control bar withdrawn — reached with `error.code=3`. H.264 differs only in which label `updateErrorText` picks (`errorSrcNotSupported`, "Video format or MIME type is not supported."). **Nothing needs to be added for `<video>` with a bad video track.***
      - ***(c) An `<audio controls>`: the forbidden shape, over the WHOLE surface, and it is the only case that still needs anything.*** *`isAudioOnly` is set at construction from `this.video instanceof HTMLAudioElement` (`videocontrols.xml:548`, `:1564`) — so the overlay suppression at `:336-339` applies to EVERY `<audio>`, whatever goes wrong, and `dynamicControls` false force-shows a bar that never fades. MP3 (`error.code=4`, measured) gets a permanent, fully-styled, live-looking transport with NO error text; `audio/ogg`, `audio/webm`, `audio/wav` and `audio/flac` are OFFERED, decode fine and die at the absent sink, reaching `MEDIA_ERR_DECODE` only after the user presses play — and the overlay is suppressed then too. Same for a `<video>` whose source is audio-only, via `loadedmetadata` (`:524-531`), which is why this criterion owns the case.*
    - ***WHAT MUST BE ADDED — one item, and it is NOT in this repo's code.*** *Levers, in order: (1) **build cubeb's ALSA backend** (`mozconfig.goanna-arm:68`), which removes the cause rather than labelling it — unchanged from T-138 and still the right fix; (2) a UXP patch gating the `isAudioOnly` overlay suppression on `hasError()`, so a dead `<audio>` says so. **(2) was considered and deliberately NOT written here.** It edits a `build/desktop/patches/` file that ships in the bundle — `push-engine-update.sh` does not push chrome, so it needs a bundle or `.ipk` push — and it re-opens the layout question the suppression exists for (Mozilla bug 619421: an overlay sized for a video box, drawn over a 28 px audio bar). With no device this session there is no way to look at the result, and a half-legible error panel is not an improvement on a dead bar. (3) A pref-level close remains impossible; ruled out above, do not re-derive.*
    - ***A BINDING ORACLE THAT NEEDS NO NAC WALK, NO PRIVILEGE AND NO DAEMON CHANGE — use this before any future `anon` run.*** *`document.querySelector('audio').getBoundingClientRect().height`. For a non-video element `nsVideoFrame::GetVideoIntrinsicSize` returns `mFrames.LastChild()->GetXULPrefSize(...).height` (`layout/generic/nsVideoFrame.cpp:614-625`) — the videocontrols box's own preferred height, ~28 px (`.controlBar { height: 28px }`, `toolkit/themes/shared/media/videocontrols.css:8-9`) when the binding attached and **0 when it did not**, because a frame-created `<xul:videocontrols>` with no binding has no children. An `<audio controls>` that is invisible is the negative; one about 28 px tall is the positive. Readable straight out of `jihad-media.html` with `gettext`.*
    - ***Off-device checks run for this task, so they are not repeated:*** *the `-moz-binding` rule ships (`device-bundle/chrome/toolkit/res/html.css:737-741`, byte-identical to source but preprocessed) and CANNOT be silently missing — `nsLayoutStylesheetCache` loads `resource://gre-resources/html.css` with `eCrash` (`layout/style/nsLayoutStylesheetCache.cpp:125-126`), so a running daemon proves it loaded; `chrome.manifest` carries `content global … contentaccessible=yes`, `resource gre-resources toolkit/res/`, the `en-US` locale and the `classic/1.0` skin; the shipped `videocontrols.xml` is byte-identical to source; and every entity it references is defined in the shipped `videocontrols.dtd` (checked by extracting both sets), so it cannot fail to parse. The XBL gates pass by construction too — `MayBindToContent` short-circuits on `IsXULElement()` and again on `IsInAnonymousSubtree()` (`dom/xbl/nsXBLService.cpp:604-624`), and the UA sheet's system principal skips the `AllowXULXBL` check (`:816-818`). **There was never a source-side reason for this binding to fail.***
    - ***STAYS `[~]`.*** *The `<video>` half is closed on both the source and the pixels. The `<audio>` half is open, it is a build-flag fix rather than a code fix, and R7's `[~]` already hangs on the same fact.)*
  - ***T-126, 2026-08-10 — RE-READ AGAINST THE "videocontrols does not attach" SCARE. Everything above SURVIVES; the scare was a broken instrument, not a finding, and this criterion is unchanged by it. Source-read only, no device.***
    - ***The premise T-126 was handed — "no controls attach, so the failure mode is a video area with NO controls at all" — is FALSE, and would have sent this criterion the wrong way entirely.*** *`NAC:controlBar absent` could not have read otherwise on any `<video>` in any build: the accessor is host-scoped and there is no `anonid="controlBar"` in this UXP. Full mechanism on this requirement's FIRST criterion, which also carries the fix to the `anon` command. The screenshot (`/tmp/jihad-vctl.png`, videocontrols' own `errorDecode` string) had already settled attachment before this task ran.*
    - ***So the degradation story is the one T-138 wrote, and it splits by ELEMENT, not by codec.*** *Stated per case because "what does a user see" was the question asked:*
      - ***(a) A playing VP8/WebM `<video controls>`: normal.*** *Picture renders, bar shows on load and auto-fades during play (`dynamicControls` true). **But there is a SECOND instance of "working-looking controls" here that nobody had written down, and it is this criterion's own forbidden shape in the audio dimension.** The mute button and volume slider are disabled only on `!video.mozHasAudio` (`videocontrols.xml:383`, `:533-536`), and `HTMLVideoElement::MozHasAudio` is just `HasAudio()` — the DEMUXED track list (`dom/html/HTMLVideoElement.cpp:198-202`), which knows nothing about output. A WebM carrying a Vorbis/Opus track therefore shows an ENABLED, draggable volume control that can never make a sound, and reports no error at all, because `OnMediaSinkAudioError` returns early whenever `HasVideo()`. **Our own asset cannot show this** — the `jihad-media.html` recipe in `docs/PICKUP.md` builds `jt.webm` from `testsrc -c:v libvpx`, i.e. video-only, so `mozHasAudio` is false and the mute button is correctly disabled. Reproducing it needs a WebM WITH an audio track, which no asset in this tree has.*
      - ***(b) An unsupported H.264 `<video controls>`: degrades visibly. Closed, and now corroborated on hardware rather than only read.*** *The screenshot on the first criterion is the same overlay machinery firing end to end — status icon, localized label, control bar withdrawn — reached with `error.code=3`. H.264 differs only in which label `updateErrorText` picks (`errorSrcNotSupported`, "Video format or MIME type is not supported."). **Nothing needs to be added for `<video>` with a bad video track.***
      - ***(c) An `<audio controls>`: the forbidden shape, over the WHOLE surface, and it is the only case that still needs anything.*** *`isAudioOnly` is set at construction from `this.video instanceof HTMLAudioElement` (`videocontrols.xml:548`, `:1564`) — so the overlay suppression at `:336-339` applies to EVERY `<audio>`, whatever goes wrong, and `dynamicControls` false force-shows a bar that never fades. MP3 (`error.code=4`, measured) gets a permanent, fully-styled, live-looking transport with NO error text; `audio/ogg`, `audio/webm`, `audio/wav` and `audio/flac` are OFFERED, decode fine and die at the absent sink, reaching `MEDIA_ERR_DECODE` only after the user presses play — and the overlay is suppressed then too. Same for a `<video>` whose source is audio-only, via `loadedmetadata` (`:524-531`), which is why this criterion owns the case.*
    - ***WHAT MUST BE ADDED — one item, and it is NOT in this repo's code.*** *Levers, in order: (1) **build cubeb's ALSA backend** (`mozconfig.goanna-arm:68`), which removes the cause rather than labelling it — unchanged from T-138 and still the right fix; (2) a UXP patch gating the `isAudioOnly` overlay suppression on `hasError()`, so a dead `<audio>` says so. **(2) was considered and deliberately NOT written here.** It edits a `build/desktop/patches/` file that ships in the bundle — `push-engine-update.sh` does not push chrome, so it needs a bundle or `.ipk` push — and it re-opens the layout question the suppression exists for (Mozilla bug 619421: an overlay sized for a video box, drawn over a 28 px audio bar). With no device this session there is no way to look at the result, and a half-legible error panel is not an improvement on a dead bar. (3) A pref-level close remains impossible; ruled out above, do not re-derive.*
    - ***A BINDING ORACLE THAT NEEDS NO NAC WALK, NO PRIVILEGE AND NO DAEMON CHANGE — use this before any future `anon` run.*** *`document.querySelector('audio').getBoundingClientRect().height`. For a non-video element `nsVideoFrame::GetVideoIntrinsicSize` returns `mFrames.LastChild()->GetXULPrefSize(...).height` (`layout/generic/nsVideoFrame.cpp:614-625`) — the videocontrols box's own preferred height, ~28 px (`.controlBar { height: 28px }`, `toolkit/themes/shared/media/videocontrols.css:8-9`) when the binding attached and **0 when it did not**, because a frame-created `<xul:videocontrols>` with no binding has no children. An `<audio controls>` that is invisible is the negative; one about 28 px tall is the positive. Readable straight out of `jihad-media.html` with `gettext`.*
    - ***Off-device checks run for this task, so they are not repeated:*** *the `-moz-binding` rule ships (`device-bundle/chrome/toolkit/res/html.css:737-741`, byte-identical to source but preprocessed) and CANNOT be silently missing — `nsLayoutStylesheetCache` loads `resource://gre-resources/html.css` with `eCrash` (`layout/style/nsLayoutStylesheetCache.cpp:125-126`), so a running daemon proves it loaded; `chrome.manifest` carries `content global … contentaccessible=yes`, `resource gre-resources toolkit/res/`, the `en-US` locale and the `classic/1.0` skin; the shipped `videocontrols.xml` is byte-identical to source; and every entity it references is defined in the shipped `videocontrols.dtd` (checked by extracting both sets), so it cannot fail to parse. The XBL gates pass by construction too — `MayBindToContent` short-circuits on `IsXULElement()` and again on `IsInAnonymousSubtree()` (`dom/xbl/nsXBLService.cpp:604-624`), and the UA sheet's system principal skips the `AllowXULXBL` check (`:816-818`). **There was never a source-side reason for this binding to fail.***
    - ***STAYS `[~]`.*** *The `<video>` half is closed on both the source and the pixels. The `<audio>` half is open, it is a build-flag fix rather than a code fix, and R7's `[~]` already hangs on the same fact.)*
  - ***2026-08-15 (LATER STILL) — ENGINE AUDIO IS AUDIBLE, HUMAN-CONFIRMED. The user at the device reported "I hear the tone" while an engine `<audio>` sine tone played through the pulse backend.*** *So HTML5 `<audio>` works end to end on hardware: ffvpx decode → cubeb PULSE backend → pulseaudio → DAC → codec output → audible. This retires the "human ear" residual below entirely — the question "does engine audio produce sound" is answered YES. It came out the headphone-routed mixer path (`SPKL DAC1` off, `Output Mixer DAC` on); auto-routing to the built-in speaker when no headset is present is a remaining audiod refinement, not a blocker. Full record: `../impl/impl-audio-backend.md` §"AUDIO IS AUDIBLE". (User also noted "the audio seeker is going crazy" — expected: a ~3 s tone on `loop=true` resets the scrubber every loop; re-check with a longer non-looping clip DONE 2026-08-15: currentTime advances smoothly 0.00→1.22→2.50→3.28→4.48→5.66 over 6 s, seeking=false throughout — the seek bar tracks correctly; the "crazy" was the 3 s loop resetting the scrubber, not a controls defect.)*
  - ***2026-08-15 (LATER) — ENGINE AUDIO OUTPUT REACHES THE HARDWARE. The cubeb PULSE backend was built, deployed and device-verified: the engine stream is unmuted at 100% and the WM8994 DAC is consuming its samples.*** *The ALSA backend below is superseded as the device audio path. Chain, all measured on device (`../impl/impl-audio-backend.md` §RESULT 2026-08-15): the pulse-native stream presents to pulse as `application.name="Jihad Browser"` (not the anonymous "ALSA plug-in" the ALSA path gave), so module-palm-policy does NOT mute it — `Volume: 100%`, uncorked — and `/proc/asound/card0/pcm0p/sub0/status` shows `state: RUNNING` with `hw_ptr` advancing ~44100/s, i.e. the DAC is clocking through our stream. Implemented as: mozconfig `--enable-pulseaudio`, pulse-0.9.22 headers staged in the sysroot (matching the device ABI — all 62 cubeb symbols verified present in the device `libpulse.so.0`), and patch `0030` targeting `JIHAD_PULSE_SINK` (cubeb dlopens libpulse; no link dep). **Residual bounded to ONE codec link + an ear (2026-08-15, read from the WM8994 mixer via `amixer -c 0`):** our DAC audio is routed to the HEADPHONE output but `SPKL/SPKR DAC1 Switch` (DAC→speaker) is OFF at rest — so it is inaudible with no headphones, and THAT (not the pulse policy, which the pulse backend already cleared) is the silence. The luna media scenario call does not flip it. FORCING those two switches on COMPLETES the chain: with `DAC1 Switch`/`Speaker Switch` on, `Speaker Volume` max, our stream at 100%, and `hw_ptr` advancing ~44100/s, the whole path decode→cubeb-pulse→DAC→speaker-mixer→speaker-amp→speaker is verified functional end to end on hardware, carrying our audio. Only literal acoustics need a human ear. **Production fix = the last link: make the daemon cause the DAC→speaker route for media** — get the stream onto the pmedia virtual sink so audiod's `onSinkChanged` sets it (debug why `JIHAD_PULSE_SINK=pmedia` fell back to pcm_output), or set the two mixer switches from the daemon (fights audiod — fallback). This retires the "engine has NO audio output" finding below.***
  - ***2026-08-15 (EARLIER) — THE BUILD-FLAG FIX WAS DONE (cubeb ALSA backend built into ARM libxul) AND THEN DEVICE-TESTED, and the answer is that ONE MORE LAYER gates it: webOS audio is policy-gated, not generic ALSA. (Superseded by the pulse-backend result above; kept for the mechanism it records.)*** *Full transcript `../impl/impl-device-2026-08-15.md`. On the TouchPad, `<audio>` for WAV and Ogg/Vorbis reports `err=0 readyState=4` (no decode error — the backend accepts the stream), and `pactl list` during playback shows 5+ pulse sink-inputs at `s16le 2ch 44100Hz` named "ALSA Playback" — so cubeb opened the PCM and connected to pulseaudio; the backend genuinely works. **But there is no sound**, because every pulse sink stays `State: SUSPENDED` and each of our sink-inputs is `Volume: 0% / -inf dB`: the hardware PCM is owned by Android's `mediaserver` HAL and webOS un-suspends/routes a sink only when its audio POLICY manager grants an app a playback "scenario" over LunaService (`com.palm.audio`/audiod). Our daemon connects as an anonymous pulse client and is muted against a suspended sink. This is the same class as "webOS Flash isn't a generic NPAPI plugin" — Flash makes sound because plugin-container runs under a recognised media policy. So the remaining work for engine audio is a **LunaService audio-scenario request**, not a cubeb or decoder change. Also corrected here: the kit/impl "bundle Jessie's libasound" decision is WRONG for this device — the Jessie 1.0.28 lib gives `err=3` (MEDIA_ERR_DECODE) against the 0.9.8 pulse plugin ABI, while the DEVICE's own `/usr/lib/libasound.so.2` gives `err=0`. Prefer the device lib (cryptofs forbids the bundling symlink anyway). MP3/AAC/H.264 decoders remain separately off.)*
    - ***STAYS `[~]`.*** *The `<video>` half is closed on both the source and the pixels. The `<audio>` half is open: the output pipe now exists and reaches pulse, but a webOS audio-policy request is the remaining gate, and it is a LunaService integration.)*
**Dependencies:** cavekit-offscreen-rendering.md (R2), cavekit-input-bridging.md (R1)

### R2: Date and time inputs are either right or off
**Description:** `<input type="date">` / `<input type="time">` behave, or are explicitly disabled.
**Acceptance Criteria:**
- [x] Established 2026-08-05 and NOT to be re-derived: **as the engine ships**, DOM-level support is present — `type` stays `"date"` (not downgraded to `text`), `value` round-trips an ISO date, and `dom.forms.datetime` defaults `true` while `dom.forms.datetime.timepicker` and `.others` default `false`. **This describes the UNCONFIGURED engine; it is no longer what Jihad ships** — see the last criterion, which turns the feature off, after which `type` is `text`. (The two read as a contradiction until 2026-08-06; they are sequential, not simultaneous.) *(Re-confirmed on device 2026-08-05 from a probe page that prints `type` and `value` back: `type=date | value=2026-08-05`, rendered as a segmented `08 / 05 / 2026` field.)*
- [x] Whether the `datetimebox.xml` binding ATTACHES is settled. *(Settled by decision rather than by inspection — see the criterion below: the feature is OFF, so the binding cannot attach, and the NAC walk is moot. Recorded in `../impl/impl-gre-widget-inventory.md` so the next person does not re-run the probe.)*
- [x] If it attaches: the picker is a XUL **popup** … It must composite and be operable, or be routed card-side the way `<select>` was. *(NOT APPLICABLE now the feature is off. Retained because it is the condition to re-check if `dom.forms.datetime` is ever turned back on.)*
- [x] Measured 2026-08-05: tapping a date field opened NO popup (`popups=0` before and after). Either that is fixed, or date/time input is turned OFF at the pref and the reason recorded — **a field that looks editable and cannot be edited is the worst of the three outcomes.** *(**Side effect, recorded 2026-08-06:** with `dom.forms.datetime`, `.timepicker` and `.others` all
false, `HTMLInputElement::ValueAsDateEnabled` is false, so the WebIDL member **`valueAsDate`
disappears from `HTMLInputElement`** — a content-visible API change, not just a widget change. A
site that feature-detects it takes a different path. Also, once the element is a plain text input
there is NO sanitisation, so a site reading `.value` expecting `YYYY-MM-DD` gets whatever was typed;
"the value survives" was only ever proven for one ISO string.)* *(RESOLVED 2026-08-05 by turning it off. `packaging/prefs/jihad-platform-prefs.js` sets `dom.forms.datetime` false with the reason next to the value; device-verified from the same probe page, which now prints `type=text | value=2026-08-05` and renders the date input identically to a plain text input. The value survives the downgrade, and the VKB and the engine's editing keys already handle a text field.)*
**Dependencies:** R1, cavekit-offscreen-rendering.md (R7), cavekit-input-bridging.md (R6)

### R3: Text-field editing keys come from the GRE, not from a hand-rolled map
**Description:** `platformHTMLBindings.xml` already maps editing keys to editor commands; we should stop duplicating it.
**Acceptance Criteria:**
- [x] `platformHTMLBindings.xml` ships in the dist and carries the standard editing set — **verified 2026-08-05: 102 handlers**, mapping `VK_LEFT`/`VK_RIGHT`/`VK_UP`/`VK_DOWN`/Home/End/Delete and the word-wise and selection variants to `cmd_*` editor commands.
- [ ] These bindings are REACHED for a focused HTML text field. They are not today: `BrowserPageGoanna::keyDown` intercepts whenever `HasFocusedEditable()` is true and drives the editor directly, so the engine's own key handling never runs. That interception was correct when synthesized key events were being dropped by `PuppetWidget` — it is not obviously correct now that they are not (cavekit-input-bridging.md R6, 2026-08-04). *(**T-109, 2026-08-10 — ANSWERED FROM SOURCE, NO DEVICE, and the answer is NO. The criterion's own premise is wrong in two ways that matter.** Nothing below was run, built or measured; every claim is a read of this tree, of the SHIPPED `build/webos-oe/device-bundle/chrome/toolkit/content/global/platformHTMLBindings.xml`, and of `third_party/uxp`.*

  ***(1) THERE ARE TWO GATES, NOT ONE, AND THE SECOND IS THE DANGEROUS ONE.*** *`GoannaRenderPage::KeyEvent` (`render/goanna/GoannaRenderPage.cpp:3006-3007`) returns early on the SAME `mFocusedEditable` condition, independently of its caller. Deleting only `BrowserPageGoanna::keyDown`'s branch — which is the edit this criterion and build-site row T-109 both describe — does NOT hand the key path to the GRE. It makes the field DEAD: `keyDown` falls through to `mPage->KeyEvent(...)`, which returns immediately, and no event is dispatched at all. Both gates have to go, together.*

  ***(2) THE 102 HANDLERS DO NOT CONTAIN WHAT THE INTERCEPTION DOES.*** *Read out of the shipped file: `inputFields`/`textAreas` cover arrows, Home/End, PageUp/PageDown, word-wise motion, the selection variants, clipboard (`accel`+c/x/v), undo/redo, `alt`+a select-all, and the emacs `control`+a/e/b/f/h/d/w/u/k/n/p set. There is **no plain Backspace, no plain Delete, no Enter, no Tab and no character insertion anywhere in it** — those live in native editor code (`third_party/uxp/editor/libeditor/TextEditor.cpp:355-417`), not in XBL. So "hand the key path back to the GRE" is TWO handovers that this criterion conflates: an XBL one covering only motion/selection/clipboard, and a native-editor one covering everything R2a says works today.*

  ***(3) THE KEYCODE NAMESPACES ARE DISJOINT, so the reachable subset of the 102 handlers is EMPTY.*** *`nsXBLPrototypeHandler::KeyEventMatched` compares `event.keyCode` against the DOM `VK_*` value resolved from `VirtualKeyCodeList.h` (`third_party/uxp/dom/xbl/nsXBLPrototypeHandler.cpp:587-612`) — `VK_LEFT` is 37. What arrives is `NpPalmKeyEvent::rawkeyCode` (`render/adapter/BrowserAdapter.cpp:1613`), forwarded VERBATIM with no translation into `nsIDOMWindowUtils::SendKeyEvent` (`GoannaRenderPage.cpp:3017`). The webOS arrows are `0xE0A0`-`0xE0A3` (device-measured 2026-07-15), and `0xE0A2 != 37`, so no arrow handler can ever fire. Every other keycode handler is unreachable for a stronger reason: the VKB has no Home, End, PageUp, PageDown or Insert key at all, and `keyIdentifierForPalmCode` in the shipped `libWebKitLuna.so` carries no End code (see the criterion below).*

  ***(4) THE MODIFIER MASK IS ALSO WRONG, and two of four bits agree only by coincidence.*** *`npPalm{Ctrl,Alt,Shift,Meta}KeyModifier` are `1,2,4,8` (`third_party/uxp/dom/plugins/base/npapi.h:652-655`); `nsIDOMWindowUtils::MODIFIER_{ALT,CONTROL,SHIFT,META}` are `1,2,4,8` (`third_party/uxp/dom/interfaces/base/nsIDOMWindowUtils.idl:268-271`). Shift and Meta line up; **Ctrl and Alt are SWAPPED.** Every `modifiers="control"` and `modifiers="accel"` handler would fire on webOS ALT and vice versa. This is a latent defect in the NON-editable fall-through path TODAY, not only a handover problem — it is unobservable solely because the VKB emits no Ctrl or Alt. Deliberately NOT fixed under T-109: it is a live-path behaviour change with no way to test it here.*

  ***(5) EVEN AFTER TRANSLATION, TWO NATIVE-EDITOR PATHS FAIL SILENTLY IN THIS UXP.*** *(a) `nsContentUtils::SendKeyEvent` sets `mKeyCode = aCharCode ? 0 : aKeyCode` and `mCharCode = aCharCode` for a keypress (`third_party/uxp/dom/base/nsContentUtils.cpp:8595-8601`); the webOS VKB puts the printable character in `key` with `chr == 0`, so a keypress built from our `(key, chr)` carries `mCharCode == 0`, `IsInputtingText()` is false (`third_party/uxp/widget/TextEvents.h:186-198`), and `TextEditor::HandleKeyPressEvent` inserts NOTHING. Typing would go dead unless the two fields are swapped at the call site. (b) Enter in a `<textarea>` needs `IsInputtingLineBreak()`, which requires `mKeyNameIndex == KEY_NAME_INDEX_Enter` (`TextEvents.h:200-207`); `SendKeyEvent` never sets it and the `WidgetKeyboardEvent` ctor leaves it `KEY_NAME_INDEX_Unidentified` (`TextEvents.h:147`), so **`sendKeyEvent` cannot express Enter-as-line-break at all here** — that half needs a UXP patch, not a call-site change.*

  ***(6) WHAT WOULD BE LOST OUTRIGHT, with no GRE equivalent.*** *Held-Backspace word acceleration (`render/goanna/BrowserPageGoanna.cpp:824`, `mBackspaceRun >= 12`): the GRE's only word-delete is Ctrl+Backspace and there is no Ctrl. The immediate `maybePaint()` after a keystroke. The `0xE0A0`-`0xE0BF` PUA swallow that keeps arrows from inserting invisible glyphs. **And the one that is a device-SAFETY regression rather than a feature loss:** Enter and Tab are deliberately deferred to `pump()` (`PEA_ENTER`/`PEA_TAB`, the F-219 rule) because they navigate, whereas the GRE submits SYNCHRONOUSLY — `HTMLInputElement::PostHandleEvent` calls `MaybeSubmitForm` inside the keypress dispatch (`third_party/uxp/dom/html/HTMLInputElement.cpp:4669-4677`) — which under a handover runs a navigation inside the YAP socket callback. That is the same class as the `clickAt` crash whose core dump stalled I/O hard enough to reboot the device. (Two things would be GAINED, stated for honesty: the `<input type=number>` sanitisation limitation and the contentEditable `textContent` fallback both go away under the real editor.)*

  ***(7) NO SAFE PARTIAL SUBSET EITHER, and the reason is the value model.*** *Our edits go through `HTMLInputElement::SetValue` (`edSetValue`, `GoannaRenderPage.cpp:994-1000`) — the programmatic setter, not an editor transaction. Hand `cmd_undo`/`cmd_redo` or the selection commands to the GRE while insertion stays ours, and the undo stack does not contain the user's typing while any GRE selection is collapsed by the next `edSetValue`. A mixed model is worse than either whole one.*

  ***VERDICT: this cannot be handed over without a device pass FIRST — and that pass is T-141, which IS runnable*** *(the on-screen VKB exists; this is not the absent-hardware-keyboard block that stops T-149). What it would have to show before any handover edit is written: (i) the raw `(key, chr, modifiers)` triple the VKB actually delivers for a printable character, Backspace, Delete, Enter, Tab and each of the four arrows, logged at `asyncCmdKeyDown` — the whole plan depends on which field carries what; (ii) that the `0xE0A0`/`0xE0A1` Up/Down labels are the recorded pair and not the binary's reversed one; (iii) that a synthesized keypress reaches the focused EDITOR, not merely a document listener — the 2026-08-04 evidence is a document `keydown`/`keyup` listener and does not prove `nsEditorEventListener` ran; (iv) that removing the `GoannaRenderPage::KeyEvent` guard does not resurrect the crash its comment names. **On (iv) there is real reason to think that guard's reason is STALE, and equally real reason not to act on it:** the crash it cites is the `mTabChild` null-deref in `PuppetWidget::DispatchEvent`, which IS on this exact path (`SendKeyEvent` sets `mIsSynthesizedForTests` because we pass `aAdditionalFlags == 0`, `nsContentUtils.cpp:8658-8660`) and IS already guarded by patch `0005` — but nobody has re-run it since. The comment's "patch 0010" is also stale numbering: that guard lives in `0005-puppetwidget-offscreen-zoom.patch`.)*
- [~] The hand-rolled keycode map in `keyDown` — the webOS `0xE0Ax` block, the Apple `0xF7xx` block, the Qt `0x0100001x` block — is reduced to only what the platform genuinely needs (the webOS VKB's own codes), with everything the GRE already handles deleted rather than duplicated. *(T-127, 2026-08-10. The Qt and Apple blocks are DELETED, and they are now **proven** unreachable rather than merely unobserved — read out of the shipped `build/webos-oe/device-binaries/libWebKitLuna.so` (unstripped), with no device involved. (1) The webOS key path is 16 bits wide end to end: `Palm::WebView::keyEvent(unsigned short, unsigned short, bool)` @0x75f760 → `PlatformKeyboardEvent(unsigned short, unsigned short, bool)` @0x540ca4 stores it with `strh …,[r7,#34]`, and `PluginView::handleKeyboardEvent` @0x4e8e68 reads it back with `ldrh …,[r2,#34]` into `rawkeyCode`, so a `Qt::Key` (≥0x01000001) cannot be represented in `key` or `chr`. (2) `WebCore::keyIdentifierForPalmCode` @0x53fc78 is the authoritative webOS code table and holds no `0xF7xx` at all; that range came from WebKit's `platform/ios/KeyEventCodesIOS.h`, compiled only by the Cocoa port. Both blocks were guesses written in f88cd932, the same commit that measured the real codes. **Stays `[~]` for the second half of the sentence:** the ASCII + `0xE0Ax` map that remains still duplicates what `platformHTMLBindings.xml` would do, and deleting THAT is the criterion above (the interception), not this one. The `0xF700-0xF8FF` swallow was KEPT against the build-site row's wording — it is a guard, not a duplicate, and the asymmetry is one-sided (one compare vs. re-opening the invisible-glyph bug if the binary read is wrong). Two things fell out, both recorded in the source: the binary reads `0xE0A0="Up" 0xE0A1="Down"`, the REVERSE of the 2026-07-15 device note the map is built on (Left/Right agree) — left unchanged on purpose, one keypress under T-141 settles it; and the PUA swallow stops at `0xE0BF`, so the real codes `0xE0E2`-`0xE0E7` (Previous/Next/Menu/Meta) still fall through and would insert an invisible glyph.)*
- [~] No regression to typing: text entry, Backspace with its accelerate-run behaviour, Enter-to-submit and caret movement all still work on device. **This is the highest-risk item in this domain** — typing is the most-used path in the browser, and the current map exists because of real device measurements. *(**2026-08-15 — DEVICE-TESTED via the inject channel; the CORE PATHS WORK, retiring the "typing might be broken" risk this box exists for. `[ ]` -> `[~]`.** On the TouchPad (`../impl/impl-device-2026-08-15.md`), on a `file://` page with an input mirroring its own `.value`: `text Hello123` → `VAL=[Hello123] len=8` (character insertion at caret, correct), one Backspace → `len=7` (correct), and `key 13` in a `<textarea>` produced a newline. So text entry, single Backspace and Enter-as-linebreak are confirmed working on this build's device binaries. **What is NOT closed, and why it stays `[~]`:** (a) accelerate-run Backspace can't be exercised through the single `inject.cmd` file — rapid writes coalesce, so five fast Backspaces deleted one char; it needs real hardware key repeat. (b) caret-by-ARROW did not move (`text abcd`, two left-arrows `key 57346`=0xE0A2, `text X` → `abcdX` not `abXcd`) — this is exactly the 0xE0A0/A1/A2/A3 keycode-map ambiguity T-127 flagged (the binary reads 0xE0A0=Up, the REVERSE of the recorded pair), and the inject `key` path may not match the adapter path anyway; settling the map is the open half. (c) a first `text` word inserted immediately after a focus-change INTO a `<textarea>` was dropped (`text Line1`/Enter/`text Line2` → `[\nLine2]`) — a focus-timing edge worth a look. The inject `key`/`text` channel is NOT the adapter path (it bypasses the VKB and the keycode map), so a full close still needs a human on the on-screen VKB; but the daemon-side insertion/editor path is now device-proven functional for the common case.)*
**Dependencies:** cavekit-input-bridging.md (R2, R6)

### R4: Pages we author use the shipped chrome widgets
**Description:** Stop hand-rolling UI that the GRE already provides.
**Acceptance Criteria:**
- [x] Any about: page or chrome UI this project ships uses what the platform provides rather than re-implementing it — the shipped bindings (`dialog.xml`, `tabbox.xml`, `groupbox.xml`, `listbox.xml`, `scale.xml`, `numberbox.xml`, `spinbuttons.xml`) where the page is a chrome window, and native HTML form controls where it is an in-content document. **Bespoke re-implementations of either are what this forbids.** *(**CLOSED 2026-08-15, and the evidence was already on file — the box had simply never been revisited after the audit.** The complete authored surface is the four rows of the T-110 audit (`../impl/impl-gre-widget-inventory.md`): `about:preferences`/`about:settings` are chrome-privileged HTML using NATIVE `<input>`/`<select>` (the in-content rule this criterion states; the `preferences.xml` strike below records why chrome-WINDOW bindings cannot apply here); `about:jihad`/`about:isis` carry no form control at all; the XPI confirm renders nothing (routes to the card); `chrome://branding/` is data. The one hand-rolled-looking shape — the Start Page Links row editor — is HTML BY RECORDED DECISION (T-140 note on the criterion below), not a bespoke re-implementation of a shipped binding. And the page now also hosts a real platform binding: `notificationbox`, proven attached and dismissable by `render/goanna/test/prefsui_test.cpp` (2026-08-15, 38 checks, offscreen widget path included), which also drove the native controls by synthesized taps. Re-opens only if a page ships a bespoke rebuild of a shipped binding — the standing rule on the criterion below governs.)*

  **`preferences.xml` was struck from this list 2026-08-05, after building the first consumer.** `prefwindow` is a *window* binding — dialog buttons, `instantApply` semantics, opened as a chrome dialog — and this embedding has no chrome window to open one in, which is the same class of assumption that broke `about:addons`. `about:preferences` is therefore in-content HTML (Basilisk's shape, Pale Moon's panes) using native `<input>`/`<select>`. That is NOT hand-rolling: `<select>`'s popup already round-trips to the card through `msgPopupMenuShow` and is device-proven in all three shells, which is a better position than an unproven XUL `<menulist>`. See cavekit-preferences-ui.md R1 for the full decision.
- [x] **`richlistbox` and `tree` are the standing preference for a XUL chrome page this project authors — and the finding, 2026-08-10 (T-140), is that there is NO first consumer, because this project authors no XUL page at all.** Closed as a recorded DECISION, not as work done. Left open as an aspiration it invites a future session to XUL-ify a working, device-proven HTML page purely to tick a box.

  **The "proven to work" half stands, and the old wording understated it.** `richlistbox` is not merely rendered by `about:addons`: populated rows AND each row's XBL-anonymous `enable-btn`/`disable-btn`/`remove-btn` were resolved and clicked on device 2026-08-04, with the effect surviving a SIGTERM restart (cavekit-addons-extensions.md R2). `tree` is `about:config`'s, operable end to end including row selection and toggling a boolean, proved from the engine's own `prefs.js` (cavekit-input-bridging.md R6).

  **But both of those pages are the GRE's, not ours.** Read out of the tree 2026-08-10 (source only, no device): outside `third_party/` and `build/` there are ZERO `.xul`/`.xhtml` documents, and every in-repo mention of `richlistbox`/`richlistitem` is documentation rather than usage — `render/goanna/README.md` and `GoannaRenderPage.cpp:746`, both describing how the inject harness addresses an *about:addons* row. The complete authored surface is the four rows of the T-110 audit in `../impl/impl-gre-widget-inventory.md`: `about:preferences`/`about:settings` (chrome-privileged HTML, zero XUL elements), `about:jihad`/`about:isis` (inline HTML from `BrowserPageGoanna.cpp:627`, no form control at all), the XPI install confirm (`jihadInstallPrompt.js` renders nothing — it routes the decision to the card), and `chrome://branding/` (data files for the GRE's own page).

  **The one place a reader WILL reach for a `richlistbox`, and why it is HTML.** `packaging/prefsui/content/preferences.js` `renderChromePane()` (`:516-580`) draws the Start Page Links editor — a variable-length list of removable name/url rows, rebuilt by a local `draw()` — as `<div class="row link-row">` plus native `<input>`s. That is exactly a `richlistbox`'s shape. Converting it means making `about:preferences` a XUL DOCUMENT, which reverses cavekit-preferences-ui.md R1 and trades a device-proven `<select>`-to-card popup path for XUL popups that are separate display roots (cavekit-offscreen-rendering.md R7). Not a trade worth making for row chrome.

  **The reason is "we chose HTML", NOT "XUL is impossible here" — do not let this be recorded as the latter.** `about:config` IS a XUL chrome document (`chrome://global/content/config.xul`, `third_party/uxp/toolkit/components/viewconfig/jar.mn:6`) and it loads: `render/goanna/test/xul_test.cpp:114` loads that chrome URL directly as phase A, with no hardware. **This makes the clause in `../impl/impl-gre-widget-inventory.md` — "a XUL chrome *document*, which this embedding has never loaded" — WRONG**; it is corrected in that file's header banner (it could not be struck in place, because that file is physically duplicated — see the banner). What this embedding has never loaded is a XUL document *we wrote*.

  **Standing rule, and the only thing that re-opens this criterion:** if a `.xul`/`.xhtml` page ever enters the tree, use `richlistbox` for row lists and `tree` for tabular data rather than rebuilding either. Until then the default for a new about: page is chrome-privileged HTML, per cavekit-preferences-ui.md R1.
- [x] A binding that does NOT work offscreen is recorded in `../impl/impl-gre-widget-inventory.md` with what failed, so the next page does not rediscover it. *(`datetimebox.xml` is the first entry with a verdict: its picker never opened on device and the feature is now off at the pref — recorded there with the measurement and the way back.)*
**Dependencies:** cavekit-preferences-ui.md (R1)

### R5: Non-blocking messages have somewhere to go
**Description:** `notification.xml` (`notificationbox`) gives in-content message bars.
**Acceptance Criteria:**
- [x] A `notificationbox` renders and is dismissable in a chrome page in this embedding. *(**T-128, 2026-08-10 — the blocking premise is SETTLED, and it is the opposite of what the build site assumed. This criterion is NOT struck.** T-128 was written expecting "the finding that struck `preferences.xml` and `findbar.xml` applies verbatim to `notification.xml`". It does not. That finding was never "chrome pages are unreachable here" — it was "there is no chrome WINDOW here" (see R4 and R6 above, which say exactly that), and `notificationbox` is not a window binding: it is a plain XUL box + stack that lives inside a document.*

  ***A chrome-privileged DOCUMENT is reachable, is already shipped, and is already device-proven — `about:preferences` IS one.*** *Read from source, not measured by me: `render/goanna/components/jihadAboutPreferences.js:34` resolves it to `chrome://jihad-prefs/content/preferences.html`; `nsChromeProtocolHandler::NewChannel2` sets the channel owner to the SYSTEM principal because that path begins `/content/` (`third_party/uxp/chrome/nsChromeProtocolHandler.cpp:178-192`); and the about: module deliberately omits `URI_SAFE_FOR_UNTRUSTED_CONTENT`, which is the ONLY thing that would have cleared that owner (`third_party/uxp/netwerk/protocol/about/nsAboutProtocolHandler.cpp:218-224`). **It is HTML MARKUP, which is a document-LANGUAGE choice, not a security one** — `IsChromeDoc()` is true for it. "HTML not XUL" and "content not chrome" are different axes and this project's notes have been conflating them.*

  *The security manager does not object either, and the reason is specific: the daemon loads with `nav->LoadURI(u.get(), LOAD_FLAGS_NONE, nullptr, nullptr, nullptr)` (`render/goanna/GoannaRenderPage.cpp:1590`) — no referrer, no triggering principal — so `nsDocShell` substitutes the SYSTEM principal for exactly that case (`third_party/uxp/docshell/base/nsDocShell.cpp:1563-1571`), the channel gate checks the triggering principal (`third_party/uxp/dom/security/nsContentSecurityManager.cpp:264-277`), and `CheckLoadURIWithPrincipal` returns `NS_OK` immediately for it (`third_party/uxp/caps/nsScriptSecurityManager.cpp:685`). The `URI_IS_UI_RESOURCE` gate that keeps WEB pages out of `chrome://` is never reached. Docshell type is not a factor — `GoannaRenderPage.cpp:1902` already records this docshell as `typeContent`, and nothing conditions chrome loading on `typeChrome`. There is also a standing hardware-free assertion: `render/goanna/test/xul_test.cpp:112-121` phase A loads `chrome://global/content/config.xul` into the content docShell and fails the run unless it loads AND paints. The DEVICE half was recorded by an earlier session, not re-run here: cavekit-preferences-ui.md R1 (re-verified 2026-08-10 on current binaries) and R3 (a pref written through the page's own `Services.prefs` call — an operation only a system principal can perform).*

  ***So T-139 is live, and nothing it needs is missing.*** *Checked file-by-file in `build/webos-oe/device-bundle/`: `chrome/toolkit/content/global/bindings/notification.xml`, its `-moz-binding` at `chrome/toolkit/content/global/xul.css:182`, `chrome://global/locale/notification.dtd` (`chrome/en-US.manifest`, `locale global en-US`) and `chrome://global/skin/notification.css` (`chrome/toolkit.manifest:24`, `skin global classic/1.0`) all ship.*

  ***Three hazards for T-139, all CODE-READ ONLY — nothing below was run:*** *(1) `about:preferences` is an HTML document, so `xul.css` is NOT applied up front; it is pulled in on demand when the first non-minimal XUL element is bound into the tree (`third_party/uxp/dom/xul/nsXULElement.cpp:815-818`), so `createElementNS(XUL_NS, "notificationbox")` from the page's own JS should work — but it has never been done in this build, and the surrounding upstream code carries an `NS_ASSERTION` saying pulling xul.css into an HTML doc "should be very rare". (2) Dismissal COMPLETES in a `transitionend` handler on `margin-top` (`notification.xml`, `_showNotification` -> `_finishAnimation`); if transitions do not fire offscreen, `_animating` stays true and the element is never removed. **Check "dismissable" by the element being GONE from the DOM, not by it looking gone** — `removeNotification(item, true)` skips the animation outright, and `skipAnimation` is also forced when `boxObject.height == 0`. (3) `javascript:` URLs do NOT execute in a chrome document here (measured 2026-08-04, `render/goanna/GoannaRenderPage.cpp:749-755`), so the box cannot be injected from outside: it has to ship inside `packaging/prefsui/content/preferences.js` or a sibling chrome page and be driven by a real tap.)* 

  ***T-139, 2026-08-10 — the consumer is BUILT and NOTHING below is verified.*** *No device, no build, no harness run; every clause here is a source read or a `node --check` on a scratch copy. `packaging/prefsui/content/preferences.{html,js}` now carry the box: an empty `<div id="msgbar">` between the pane switcher and `#content` (OUTSIDE `#content`, which `renderPane()` empties on every switch), and `initNotifyBox()` creating it with `createElementNS(XUL_NS, "notificationbox")` at DOMContentLoaded. Two consumers, both on a real tap: `resetPane()` reports a bulk restore (INFO_MEDIUM) and is the one that fires on every use of the footer button; the new `echoWrite()` reports a pref write that did NOT take (WARNING_HIGH), the case where the control is left showing a value that is not in effect. Every message is ALSO mirrored into the existing `#status` footer line, so a build where the binding never attaches is no worse off than before this landed.*

  ***A FOURTH HAZARD, not in the T-128 list above, and it is the one most likely to produce a FALSE NEGATIVE for this criterion:*** *the XBL binding is installed during FRAME CONSTRUCTION, not during `appendChild`. A box created and used in the same JS turn can have no `appendNotification` at all, and a session that hit that would report "the binding does not attach in an HTML document" when the truth is "you asked too early". Handled two ways: the box is built at load rather than at first message and `getBoundingClientRect()` forces the flush that constructs its frame, and `notifyBox()` tests for the METHOD rather than the element on EVERY use, falling back to the footer line. `notification.xml` is a chrome URI and `nsXBLService::FetchBindingDocument` forces a SYNC load for those (`third_party/uxp/dom/xbl/nsXBLService.cpp:960-961`), so the flush should be enough — code read, not run.*

  ***Hazard 1 is answered from the sheets, and better than the note assumed.*** *`minimal-xul.css` — already loaded for EVERY HTML document — is `@namespace XUL` with `* { display: -moz-box; }`, so the box lays out even before `xul.css` arrives; and pulling `xul.css` in cannot restyle this page, because its default namespace is XUL and its only two `html|`-prefixed rules are inside a XUL `textbox` (`third_party/uxp/toolkit/content/xul.css:778,782`). The cost is that sheet's memory on a 512 MB device, paid only while about:preferences is open.*

  ***Hazard 2 is answered by NOT offering the platform close button.*** *`notification.xml`'s `messageCloseButton` calls `dismiss()` -> `close()` -> `removeNotification(item)` with **no** `skipAnimation`, i.e. exactly the `transitionend`-on-`margin-top` path this note warns about, so it is hidden with `hideclose="true"`. The only affordances are a `Dismiss` button (whose callback returns TRUE, which suppresses the binding's own animated `close()`) and a plain-DOM `click` listener on the bar itself; both call one idempotent remover that uses `removeNotification(item, true)` and then checks `item.parentNode`. **Do not "fix" this by letting the X run and sweeping the element out afterwards:** that leaves `_animating = true` with `_closedNotification` pointing at a detached node, and the NEXT `appendNotification` throws inside `_finishAnimation` when `removeChild` is called on a non-child. That is why the button is hidden rather than merely backstopped.*

  ***Hazard 3 is OVERTAKEN — no new daemon code is needed to verify this criterion.*** *`DebugElementRect` already accepts `sel:<css>` and `anon:<css>|<anonid>` beside a plain id (`render/goanna/GoannaRenderPage.cpp:855-868`), so `clickid` can tap a XUL element. The whole criterion is answerable from the inject channel, and the last step is literally the DOM test this criterion asks for, because `anon` prints `?` when `querySelector` finds nothing:*

  ```
  url about:preferences
  anon notificationbox                     # expect xbl=1: stack  -> xul.css + XBL attached
  clickid reset-pane                       # real tap on "Restore this pane's defaults"
  anon notification                        # expect xbl=1: hbox   -> the bar RENDERED
  clickid sel:button.notification-button   # real tap on Dismiss
  anon notification                        # expect ?             -> GONE from the DOM
  ```

  ***DEPLOY TRAP, verified by reading the scripts:*** *`push-engine-update.sh` copies only libxul, the daemon and `goanna.js` (`:43-44`) — it does NOT ship `packaging/prefsui/`. That comes from `make-device-bundle.sh:194-220`, so this needs a bundle/`.ipk` push, or a per-variant push of the two files into `deviceroot/hl/prefsui/content/`. Testing this after a `push-engine-update.sh` would measure the OLD page and read as "the box does not render".*

  ***One unrelated defect found while reading, NOT fixed, recorded so the new bar is not blamed for it:*** *`saveChrome()` swallows its write failure (`preferences.js:345-348`, "a read-only profile must not break the rest of the page") and every caller then reports success unconditionally — `status("Home page saved.")`, `status("Start page links saved.")`, and the Browser-pane reset. The Browser pane can already claim to have saved something it did not. The fix is a status return from `saveChrome` plus three branches, and it belongs with the criterion below, not with T-139.*

  ***T-139 was RUN, 2026-08-15, and this criterion is now `[x]` — every clause above that said "code read, not run" has been measured.*** *`render/goanna/test/prefsui_test.cpp` + `build/desktop/build-prefsui-test.sh` load the real `about:preferences` in the desktop harness and drive the page's own controls with SYNTHESIZED TAPS. **38 checks, 0 failures, exit 0 — run TWICE, once on the GTK-window widget and once on the offscreen PuppetWidget path (`JIHAD_OFFSCREEN=1`) which is the one the device runs, with byte-identical results including every rect.** Full record: `../impl/impl-gre-widget-inventory.md`, section dated 2026-08-15.*
  - ***The binding attaches, in an HTML document.*** *`DebugAnonNodes("#msgbar notificationbox")` → `NAC:1 xul:stack[1] | xbl=1: xul:stack` — the binding's own anonymous `<stack>`. **Hazard 1 and hazard 4 are both answered by measurement and neither was a problem**: `xul.css` came in on demand and the bar laid out, and `initNotifyBox()`'s `getBoundingClientRect()` flush was enough that `xbl=1` was already true before any message was raised.*
  - ***It renders.*** *A real tap on `#reset-pane` (`resetPane()`, the page's own consumer) produced `notification[value="prefs-reset"]` at rect **599x36**, carrying the exact expected `label`, with `hideclose="true"` present and `button.notification-button` rendered at 101x29. **"Renders" here means a laid-out frame with the right attributes and bound anonymous content, judged from layout and the DOM — NOT from pixels**, because desktop-harness pixel readback is a recorded dead end (`../impl/dead-ends.md`) and a DOM criterion should not be answered from a frame buffer. A device screenshot is what would close the pixel question, and it is the one thing left.*
  - ***It is dismissable, and the bar is GONE FROM THE DOM.*** *A real tap on Dismiss, and separately a real tap on the bar itself, each leave `sel:#msgbar notification` → `(no element)`. `#status` carries the message before and after. The bar can be raised a second time afterwards, which is the state this note warns can be corrupted.*
  - ***Hazard 2 is CONFIRMED RELEVANT and correctly handled — the bar's height is 36, NOT 0.*** *So `_showNotification`'s `skipAnimation` shortcut (`height == 0`) did not apply and the append took the animated slide-in path; dismissal completed anyway because the page only ever calls `removeNotification(item, true)`. **What this run cannot separate, and must not later be claimed to have:** `dismissNotification()` deliberately pairs that call with a `parentNode` sweep, and from outside the page the two are indistinguishable. What is proven is the outcome this criterion names — gone from the DOM, not merely invisible.*
  - ***Hazard 3 is CONFIRMED and BROADER than it was written.*** *The 2026-08-04 measurement was against `about:addons`, a XUL document. Repeated here against a chrome-privileged **HTML** document: `DebugRunChromeJs("javascript:void(document.title='chrome-jsurl-ran')")` returns and the title does not change. **So the finding is about CHROME DOCUMENTS, not about XUL** — and it has a second consequence nobody had noticed: `GoannaRenderPage::ScrollTo` drives `window.scrollTo` through exactly such a URL, so **it is a no-op on a chrome page**. That cost this test a whole run — `#reset-pane` sits at y≈854, the viewport was 768 high, the scroll silently did nothing and the tap landed on `<null>`, which reads as "the button does nothing". The harness grows the viewport with `Resize()` instead.*
  - ***The instrument was proved able to fail.*** *`JIHAD_PREFSUI_NEG=139` (look for a notification value the page never emits) → 1 failure, exit 4.*
- [x] It is used where a message does not deserve a modal: "add-on installed", "cookies cleared", "this page tried to open a popup". Today the ONLY way to tell the user anything is `msgDialog*`, which BLOCKS the daemon until the card answers (cavekit-browser-services.md R3) — a heavy price for an informational line, and a deadline risk for anything the user ignores. *(**CLOSED 2026-08-15 — DEVICE-VERIFIED end to end. Was `[~]` (daemon measured, card unrun) earlier the same day.** On the TouchPad: `luna-send -P` (public bus) `clearCookies` → daemon logged `notify: privacy: Cookies cleared. (pushed)` and the Enyo card rendered the toast — **it is visible in the device screenshot** (a black transient bar reading "Cookies cleared." over the start page; `../impl/impl-device-2026-08-15.md`). So all three device unknowns the toast note named are answered YES: `LSSubscriptionAdd`/`Reply` exist and work on this device, the card's subscribe arrives on the public bus, and the toast composites over the card. Pushed via `push-card-js.sh enyo` (stamp-proven; Browser.js must be in the file set). Card half of Mochi/Mojo written but not re-run this session — the mechanism is identical and the Enyo proof carries it. Full record: `../impl/impl-toast-channel.md` + `../impl/impl-device-2026-08-15.md`; the call-site classification table is in `../impl/impl-gre-widget-inventory.md`.*

  ***THE CHANNEL IS NOT A `notificationbox`, and that is a decision rather than an oversight.*** *A `notificationbox` lives inside a chrome DOCUMENT we author, and the only one we ship is `about:preferences` — which is not on screen when any of the three named messages happens. "Cookies cleared" is raised by a button in the CARD's own Preferences view; an add-on installs while an arbitrary CONTENT page is displayed. A bar in a page nobody is looking at is not a message. So the criterion above keeps its `notificationbox` consumers (`resetPane`, `echoWrite` — T-139, closed) and this one is served by a **card-side toast fed by a daemon Luna SUBSCRIPTION**, which is the only surface that exists in all three shells at the moment these messages arise.*

  ***WHY LUNA AND NOT A NEW YAP MESSAGE.*** *R1's second criterion in cavekit-ipc-contract.md permits an addition only with its own USER AUTHORISATION, and none exists for this. Luna needs no contract change and has direct precedent in this daemon: `getChromePrefs` was added on 2026-08-06 the same way. **Nothing in the frozen surfaces moved** — `check-yap-contract.sh` inside `build-daemon-arm.sh` reports "73 commands (incl. the 0x1600 Jihad addition), 58 messages, both sides agree" on this tree, and no card gained a `callBrowserAdapter` method (cavekit-ui-shell.md R2, cavekit-mojo-ui.md R3 both untouched).*

  ***WHAT MOVED, and it is exactly ONE existing call site.*** *`components/jihadInstallPrompt.js`'s incompatibility notice (the T-103 "clear reason") travelled as `msgDialogAlert`. An alert nobody can act on is a statement, not a question, and it could park the daemon in `awaitDialogReply` for the length of the dialog deadline. It now posts `jihad-notify` instead. **Everything else stayed modal on purpose** — content `alert`/`confirm`/`prompt`, HTTP auth, the XPI install confirm and the SSL confirm are all questions whose answer changes what happens next, and `window.alert` additionally owes content a paused script. The per-site table is in the impl doc.*

  ***WHAT WAS ADDED, because two of the three named examples had NO emitter at all.*** *`clearCache`/`clearCookies` were completely silent — a user tapped the button and nothing on screen changed — so the acknowledgement is raised inside `jihad::ClearCache`/`ClearCookies` themselves, where BOTH callers (the frozen YAP commands and this variant's Luna service) pass through it and neither can be the silent one. "Add-on installed" likewise had no emitter: `jihadInstallPrompt.js` now also observes `addon-install-complete` (`amWebInstallListener.js:220`, whose `installs` are the ones that SUCCEEDED).*

  ***"THIS PAGE TRIED TO OPEN A POPUP" IS DELIBERATELY NOT BUILT, and the reason is not "it is impossible".*** *The engine does raise `DOMPopupBlocked` — a trusted DOM event dispatched at the document by `nsGlobalWindow::FirePopupBlockedEvent` (`dom/base/nsGlobalWindow.cpp:8423-8449`), reached through `FireAbuseEvents` (`:12521`) on exactly the path `SetBlockPopups(true)` arms. **Nothing in this daemon listens for it**, so today the message does not exist and inventing UI for a non-event was out of scope. Building it is a chrome-side event listener plus an `isTrusted` filter, and it has ONE design question that cannot be answered off-device: an ad-heavy page blocks popups in bursts, so it needs rate-limiting or coalescing or it becomes a toast storm. Recorded so the next session neither re-derives the mechanism nor assumes it was overlooked.*

  ***MEASURED, host only — no device was available, and every claim below is a harness run.***
  - ***The moved call site: `xpi_mismatch_test`, 8 checks 0 fails, exit 0.*** *`dialogs=0 notifications=1`, text `"Jihad T103 Mismatch could not be installed because it is not compatible with Jihad Browser 1.0."`, category `addon`. **Both halves are asserted** — the notification arrives AND no dialog is raised — because asserting only the first would pass a regression that raised both.*
  - ***The added emitters: `services_test`, all five new checks PASS.*** *`clearCache`/`clearCookies` produce exactly two notifications (`Cache cleared.`, `Cookies cleared.`, both category `privacy`) and ZERO dialogs, and the flow completes. The install-success notice is proved by a new `JIHAD_XPI_ACCEPT=1` mode of the same test: 6 checks 0 fails, `page status=0`, one notification `"Jihad T103 Good installed."`, no alert.*
  - ***The degrade path is exercised, not argued.*** *With the sink detached — the desktop case, and equally a device whose `liblunaservice.so` turns out to lack `LSSubscriptionAdd` — a post is a no-op that writes ONE line (`[jihad-bs] notify (no channel): privacy: Cookies cleared.`) and returns: no crash, no dialog, nothing delivered. The desktop daemon builds, starts and logs `luna: liblunaservice.so unavailable … — no Luna service`.*
  - ***Controls, because an assertion that cannot fail proves nothing.*** *`JIHAD_SVC_NEG=148` (expect texts no build emits) turns 1 pre-existing failure into 3. `JIHAD_XPI_GOOD=1` (matching add-on, declined) gives 1 confirm, 0 alerts, 0 notifications — so the observer's `appDisabled` filter is doing real work.*
  - ***The ARM daemon cross-builds clean*** *and the YAP contract guard passes, which is the proof that this widened nothing.*
  - ***Honest note on `services_test`:*** *it still exits 4. Its PIXEL half (`green=0 blue=0`) was **already failing at HEAD** — verified by rebuilding the unmodified file from git and running it — and is the recorded desktop-harness readback dead end, not a regression from this work.*

  ***WHAT IS DEVICE-GATED, i.e. what stops this being `[x]`.*** *Nobody has seen a toast. Three things are unverified and each could independently be wrong:*
  1. ***The subscription symbols.*** *`LSSubscriptionAdd`/`LSSubscriptionReply` are dlsym'd and NULL-checked, but no device was available to confirm this `liblunaservice.so` exports them. Their signatures came from the webOS 3 sources in this workspace (`luna-sysmgr/Src/base/DisplayManager.cpp:1836`, `AmbientLightSensor.cpp:403`), which are LS1-era CALLERS — the same class of source that got `LSMethod`'s third field wrong. Neither takes a struct by value, which is why that specific failure cannot recur here, but presence is still an assumption.*
  2. ***That a card's subscribe actually arrives.*** *The method is registered on the PUBLIC bus for the measured reason (cards are public-bus callers, 2026-08-06) and the role files already ship, so this should work exactly as `clearCookies` does — but "should" is what the private-only registration also looked like before it was measured.*
  3. ***That the toast composites over the WebView.*** *The precedent is good — the `<select>` popup is card-side HTML drawn over the same NPAPI plugin surface and is device-proven — but it is a precedent, not a measurement of this element. If the toast is invisible on device, the answer is to draw it inside the shell's own chrome band rather than over the plugin, NOT to reach for a platform banner (Mojo's banner goes to the system dashboard, which is the wrong surface for a message about the page in front of you).*

  ***One device run closes all three:*** *launch each variant, tap Preferences -> Clear cookies, and look. The daemon log says which half failed — `notify: privacy: Cookies cleared. (pushed)` means the daemon did its part and the card did not.)*
**Dependencies:** R4

### R6: The find bar is the GRE's, and the real blocker is named
**Description:** `findbar.xml` is a complete find UI we do not use.
**Acceptance Criteria:**
- [x] Recorded, so nobody mistakes the UI for the problem. **The diagnosis this criterion used to carry was WRONG and is corrected here** — it said `FindNext` SIGSEGVs because it dereferences "a frame-selection controller the offscreen browser never sets up", and that sent this port down a two-week dead end. The real cause: `nsWebBrowserFind::SearchInFrame` runs a same-origin check through `nsContentUtils::SubjectPrincipal()`, which opens with `MOZ_CRASH("Accessing the Subject Principal without an AutoJSAPI on the stack is forbidden")` when there is no JSContext. An EMBEDDER calls `FindNext` from C++ with no script on the stack, so it aborts — and `MOZ_CRASH` presents as SIGSEGV at address 0, which is exactly the fault that was misattributed. **The selection machinery was fine all along; the crash happened before any of it.** **Now proven by an automated gate (2026-08-06).** `render/goanna/test/find_test.cpp` used to PRINT
the result of `page.Find(...)` and assert nothing, with `ok = freezeOK` and a comment restating the
old wrong expectation — so the binary could not fail because of find, whatever find did. It now
asserts BOTH directions and gates the exit code on them. Run on the desktop harness:
`[find] findString hit=1 miss=0 ok=1` — a present string is found, an absent one is not. **This is
the first repeatable, hardware-free proof that find works**, and it replaces the device anecdote.
*(Same run reports `freezeOK=0` — `painted load=0 freeze=0 thaw=0`. That half was already failing
before this change was made and is NOT find-related; it is most likely the container having no
display/shm rather than a real regression, but it is unverified either way and is tracked in
cavekit-offscreen-rendering.md, not here. The binary exits 4 on that half.)*
Fixed by consulting the subject principal only when a JSContext exists (patch 0015, `third_party/uxp/embedding/components/find/nsWebBrowserFind.cpp`, which carries the full explanation inline). Find-in-page now works.
- [x] The find UI is taken from `findbar.xml` rather than rebuilt. *(RESOLVED AS OUT OF SCOPE, and the premise is void — it was conditioned on "once an offscreen-safe selection controller exists", which was never the blocker. `findbar.xml` is chrome-WINDOW UI, the same category as `browser.xml` and `autocomplete.xml` already excluded above: there is no chrome window here, and the find bar is card-side in all three variants for the same reason the `<select>` dropdown is serialized to the card — the isis/Atlas model, cavekit-ui-shell.md R5. Moved to Out of Scope below.)*
**Dependencies:** cavekit-ui-shell.md (R4)

### R7: Anything disabled is disabled on purpose
**Description:** No half-working web-platform features.
**Acceptance Criteria:**
- [~] Every content-facing capability in this domain is either working, or turned off at a pref with the reason recorded in `packaging/prefs/jihad-platform-prefs.js` next to the value. *(**T-138, 2026-08-10.** Date/time inputs: OFF at the pref with the reason next to the value (R2). HTML5 media: DECIDED and recorded in the same file — `<video>` stays ON because VP8/WebM plays, with `media.webm.enabled` pinned as the one working path and the full reasoning beside it. **Stays `[~]` for `<audio>`, which is the one capability that is neither working nor turnable off:** the engine has NO audio output at all (cubeb compiled with no backend) yet still advertises `audio/ogg`, `audio/webm`, `audio/wav` and `audio/flac`, because those decoders are gated on a pref and the PDM only, never on the output. The codec prefs are CONTAINER-scoped, so the two containers that also carry video cannot be turned off without taking Theora and VP8 with them — a pref-only close is impossible, and half-flipping would only make the surface inconsistent. Full analysis on R1's last criterion; the fix is `--disable-alsa` in `build/webos-oe/mozconfig.goanna-arm:68`, not a pref.* ***2026-08-15 — THE BUILD-SIDE HALF IS DONE, and the sentence above is now stale in one word: the engine no longer has "NO audio output at all".*** *`--enable-alsa` is set, `cubeb_alsa.o` is compiled into the ARM libxul, and the reasoning was updated in `packaging/prefs/jihad-platform-prefs.js` where this criterion requires it — the paragraph that used to assert "no engine audio can EVER play" now says what is true instead of being quietly deleted. **Stays `[~]` and the reason is narrower than before:** `<audio>` is no longer "neither working nor turnable off" on the build's terms, but nothing has been heard on hardware, so it is not yet demonstrably *working* either — it has moved from a known hole to an unverified fix. The half of this criterion that was always a decoder problem is unchanged and unaffected: `audio/mpeg` is still refused for lack of an MP3 decoder, not for lack of output. Evidence and the device-run checklist: R1's last criterion and `../impl/impl-audio-backend.md`.)*
- [x] The prefs file stays the single shared source appended by BOTH builds, so desktop and device cannot disagree — the rule established when `dom.w3c_touch_events.enabled` was found off on a touchscreen-only device (cavekit-input-bridging.md R3). *(Verified 2026-08-05: `build/desktop/build-goanna.sh` (PLATFORM_PREFS) and `build/webos-oe/make-device-bundle.sh` both append `packaging/prefs/jihad-platform-prefs.js`, and the datetime decision above went into that one file rather than into either build.) **Downgraded on review 2026-08-06:** true of `jihad-platform-prefs.js`, but NOT of the whole pref surface — `make-device-bundle.sh` also appends ~16 device-only prefs inline (the low-RAM tuning block), three of which back `about:preferences` rows, so those rows read "not available" on desktop. ~~And the desktop build installs neither the `prefsui` package nor the about: module, so `about:preferences` does not exist there at all.~~* ***Second half CORRECTED 2026-08-10 — it was overtaken and never updated.** `build/desktop/build-goanna.sh` now copies `packaging/prefsui/content`, installs `render/goanna/components/jihadAboutPreferences.js`, writes the about-preferences manifest, registers both manifests in `chrome.manifest`, and FAILS LOUD if any input is missing — its own comment cites this requirement as the reason. So `about:preferences` DOES exist on desktop. ~~What still stands, and is worse than recorded: `make-device-bundle.sh` appends device-only prefs inline, and at least NINE of them back `about:preferences` rows, not three. That inline block is the whole remaining gap for this criterion.~~* ***SPLIT DONE IN SOURCE 2026-08-10 (T-111) — and the count above is wrong in a way worth reading before the fix.*** *Read from the tree; **no device and no build was run**, so nothing here is a build or device result. Intersecting the inline heredoc's pref names with the `pref:` keys in `packaging/prefsui/content/preferences.js` gives **ELEVEN** row-backing prefs, not nine. **But only THREE of them ever read "not available" on desktop.** `prefExists()` is `getPrefType() != PREF_INVALID` (`preferences.js:357`), and grepping `pref("<name>"` across `third_party/uxp` shows only `general.smoothScroll` (the GRE ships its `.mouseWheel`/`.pixels`/… sub-prefs but not the master bool — that is a Firefox app pref), `browser.sessionhistory.max_entries` and `browser.cache.memory.capacity` (default COMMENTED OUT at `all.js:71`) have no GRE default. The other EIGHT — `layout.frame_rate`, `image.animation_mode`, `network.dns.disablePrefetch`, `network.prefetch-next`, `network.http.max-connections`, `browser.cache.disk.{enable,capacity,smart_size.enabled}` — have upstream defaults, so their rows were never disabled on desktop: they silently showed the **STOCK** value (frame_rate -1 not 30, disk capacity 256000 not 51200, prefetch-next true not false). **So the 2026-08-06 wording was RIGHT about "three read not available" and the 2026-08-10 correction changed the number while keeping the verb, making the sentence wrong about all eleven.** The quiet case is the worse one: a disabled row announces itself, a stock value does not.* ***What changed:*** *all eleven moved into `packaging/prefs/jihad-platform-prefs.js` — the file BOTH builds already append — so `build/desktop/build-goanna.sh` needed NO change and no third append site was created that could itself drift. `make-device-bundle.sh` keeps 16 prefs inline DELIBERATELY, and the rule is written into both files: none of them backs an about:preferences row, and they are 512 MB / ARM numbers (`image.mem.surfacecache.*`, the four `javascript.options.mem.*`, `media.cache_size`/`media.memory_cache_max_size`, `browser.sessionhistory.max_total_viewers`, `browser.cache.memory.enable`, `nglayout.initialpaint.delay`, `network.http.max-persistent-connections-per-server`, `toolkit.storage.synchronous`). Appending those to the desktop harness would be wrong on its own terms: a harness that GCs at a 32 MB heap and caps its surface cache at 32 MB is not standing in for the device, it is simulating a memory ceiling the host does not have.* ***Dry-run checks, on scratch copies:*** *the DEVICE pref set is unchanged — the same 33 `pref(name, value)` calls, only comments differ; no pref name is defined twice, so goanna.js last-wins never arbitrates; `bash -n` passes on the edited bundler and the edited prefs file parses.* ***Stays `[~]` for two reasons.*** *(1) Neither build has been run and no `.ipk` was produced, so "desktop shows the same eleven rows" is an argument, not an observation — one desktop-harness run of `about:preferences` closes it. (2) The two builds re-append ASYMMETRICALLY: `make-device-bundle.sh` does `rm -rf "$OUT"` (line 22) and re-copies `goanna.js` every run so its marker guard can never fire, while `build-goanna.sh` appends IN PLACE into `dist/bin/goanna.js` under `grep -q 'JIHAD platform prefs'` — if a `mach build` does not rewrite that file, an edit to the shared file is silently not re-appended and the marker still says it is there. Check for the VALUE, not the marker. Its two pref appends are also `[ -f "$DISTBIN/goanna.js" ]`-guarded with a SILENT skip, unlike the prefs-ui block above them which FAILS LOUD; hardening that is the next small piece of this criterion.* ***THAT PIECE IS DONE — 2026-08-15 (T-111 remainder), and it was TESTED rather than only inspected.*** *`build/desktop/build-goanna.sh`'s two appends are now one block, and both defects named above are closed:*
  - ***Silent skip → FAILS LOUD.*** *Missing shared pref file, or a missing `dist/bin/goanna.js`, now prints the reason and `exit 1`s, matching the prefs-ui block directly above. Both paths were exercised: a bad `PLATFORM_PREFS` path and a deleted `goanna.js` each exit 1 with a specific message.*
  - ***Marker guard → VALUE check, exactly as this criterion demanded.*** *The `grep -q 'JIHAD platform prefs'` guard is gone. The block now strips whatever a previous run appended, re-appends both shared files unconditionally behind a sentinel line, and then verifies that the region after the sentinel is **byte-identical** to the two source files concatenated (`cmp`), failing loud with a diff if not. It asks what the dist CONTAINS instead of whether something once claimed to append it, so an edit to a shared pref file cannot fail to reach the dist while the marker still says it did.*
  - ***Measured on a scratch dist rather than argued (the full desktop build was not run — it is not needed to exercise this block):*** *fresh run appends 34 pref lines and verifies; a second run is byte-for-byte idempotent (sentinel count 1, identical line count, no duplication); a pref added to the shared file REACHES the dist on re-run and is REMOVED again when reverted — which is precisely what the old marker guard could not do; and a dist appended by the OLD code (no sentinel) is cleaned up rather than duplicated, landing at the same line count as a fresh run. `bash -n` passes.*
  - ~~***What is still open on this criterion, unchanged and now the ONLY thing:*** *reason (1) above — neither build has been RUN end to end, so "desktop shows the same eleven rows" is still an argument, not an observation. One desktop-harness run of `about:preferences` closes this criterion.*~~ ***THAT RUN HAPPENED — 2026-08-15 (T-111, observation half). CRITERION CLOSED `[x]`: reason (2) was fixed above last wave, and reason (1) is now an observation.*** *`render/goanna/test/prefsui_test.cpp` loaded `about:preferences` on the desktop dist and read back all eleven rows. **None reads "not available"** — the page reports its own missing set through `class="row unavailable"`, and `.row.unavailable .row-pref` resolves to `(no element)` on BOTH panes that carry these rows (`general`, `advanced`), while every one of the eleven pref names is present in the rendered pane text. The three this criterion names as SILENTLY STOCK before the split are all correct: **`layout.frame_rate=30`** (not -1), **`browser.cache.disk.capacity=51200`** (not 256000), **`network.prefetch-next=false`** (not true); and so are the other eight (`general.smoothScroll=false`, `browser.sessionhistory.max_entries=20`, `browser.cache.disk.enable=true`, `browser.cache.disk.smart_size.enabled=false`, `browser.cache.memory.capacity=16384`, `network.http.max-connections=32`, `network.dns.disablePrefetch=true`, `image.animation_mode=once`). Same result on the GTK and the offscreen-PuppetWidget widget paths. The hardened append block above was exercised against a REAL dist for the first time in the same session, reporting "34 pref lines re-appended to the desktop goanna.js, verified byte-identical to the source files".*
  - ***One trap this run walked into, recorded because it produces a convincing FALSE NEGATIVE:*** *the answer depends on the PROFILE. `build/desktop/out/.jihad/default/profile/prefs.js` carries `user_pref("browser.cache.disk.capacity", 358400)`, left over from before `browser.cache.disk.smart_size.enabled` was pinned false and the cache autosized itself from free disk. A user value beats every default, so a run against that profile reports 358400 and reads as the pref split having failed. **It has not; that is one stale profile.** `build-prefsui-test.sh` `rm -rf`s a dedicated `JIHAD_STATE_DIR` every run, because this criterion is about what the BUILD ships.*
  - ***The instrument was proved able to fail:*** *`JIHAD_PREFSUI_NEG=111` adds a pref no build ships to the eleven → 2 failures, exit 4.*
  - ***What this does NOT claim.*** *`make-device-bundle.sh` was not run this session (novacom down; `build/webos-oe/` is another agent's this wave). Its half rests on last wave's dry-run — same 33 `pref()` calls, only comments differ — plus the structural argument that it `rm -rf`s its output and re-copies `goanna.js` every run, so its append cannot go stale the way the desktop one could. The asymmetry this criterion was downgraded for was the DESKTOP side, and that side is now both hardened and observed.*
**Dependencies:** R1, R2

## Out of Scope
- Bindings that cannot apply to this embedding — `browser.xml`, `remote-browser.xml` (we ARE the
  browser; there is no second process), `autocomplete.xml` (the URL bar is card-side in all three
  variants), `editor.xml`, `wizard.xml`, `filefield.xml` (no platform file picker).
- The `<select>` dropdown, which is deliberately NOT a GRE popup here — it is serialized to the
  card (cavekit-ui-shell.md R5), the isis/Atlas model.
- `findbar.xml`, for the same reason (moved here from R6, 2026-08-05). It is chrome-window UI;
  the find bar is card-side in all three variants. The engine side of find — `FindNext` — works
  as of patch 0015; what was recorded as its blocker was a misdiagnosis, see R6.
- The preferences PAGE itself — cavekit-preferences-ui.md. This domain supplies its parts.

## Cross-References
- See also: `../impl/impl-gre-widget-inventory.md` (the full 49-binding inventory and the
  per-binding verdicts), cavekit-preferences-ui.md (R4's first consumer),
  cavekit-offscreen-rendering.md (R7 — popup compositing, which R2's picker needs),
  cavekit-input-bridging.md (R2/R6 — the key path R3 wants to hand back to the engine),
  cavekit-ui-shell.md (R4 — findInPage, which R6 depends on).

## Changelog
- 2026-08-15: R5's last criterion moved `[ ]` -> `[~]` (T-146 + T-148). A non-blocking message
  channel now exists — a card-side toast fed by a daemon Luna subscription — and the one
  informational `msgDialog*` call site moved onto it; two of the three named examples ("cookies
  cleared", "add-on installed") had no emitter at all and now do. The daemon half is measured on
  the desktop harness; the card half is device-gated. `../impl/impl-toast-channel.md`.
- 2026-08-05: Initial draft, from the user requirement *"bring in all useful widgets as goals"*
  after an inventory found 49 shipped bindings and only a handful in use. The four CONTENT-facing
  bindings are the reason this is a domain rather than a checklist: `videocontrols`, `datetimebox`
  and `platformHTMLBindings` apply to every website, and no requirement anywhere mentioned them.
  R3 is the one with teeth — the GRE already maps 102 editing-key handlers that our own `keyDown`
  currently prevents from ever running.
