---
created: "2026-06-30"
last_edited: "2026-08-15"
---

# Cavekit: Input Bridging

## Scope
Translating webOS input commands (delivered over YAP) into DOM input events the
engine understands: pointer/click, keyboard, touch, gestures, and drag-scroll,
with correct coordinate mapping under zoom and scroll. Reference:
`render/goanna/PORT-MAP.md` (input rows), `docs/IPC-CONTRACT.md` (input commands).

## Requirements

### R1: Pointer/click input
**Description:** Click and hold commands hit the right element.
**Acceptance Criteria:**
- [x] `clickAt(x,y,numClicks)` dispatches pointer/mouse events at the content coordinate; the element at that point receives them (verified via a page that reports the event target, or by a navigation that results from clicking a link).
- [x] Click count (single/double) is preserved.
- [x] `holdAt` produces the long-press/context behavior.
- [x] Input handlers do NOT perform navigation or other engine teardown synchronously inside the YAP socket callback; teardown-capable work (hit-test/activation/click/navigation) runs in the page-lifetime-protected pump/tick context. *(review #5 H-1: a synchronous link-navigation from the clickAt callback re-entered and crash-rebooted the device.)*
- [x] Tapping a **checkbox/radio** toggles it, and a page click handler (incl. framework/React click-driven onChange, which reads `.checked` AT click time) observes the NEW state. *(2026-07-20: root cause — a synthesized `SendMouseEvent` down+up fires the page's `onclick` but does NOT run the click's DEFAULT ACTION in this embedding, so links [handled by direct-navigate] and JS `onclick` worked while native control activation silently did nothing. Fix: hand-flip `checked` via `nsIDOMHTMLInputElement::SetChecked` (radio → true, maintains the group; crash-safe — a plain DOM setter, no JSContext) BEFORE the synthetic click [F-001, so click/onChange handlers see the new state, matching a real browser's pre-phase toggle], then dispatch bubbling `input`+`change` Events AFTER the click. `GetDisabled` [not just the attribute] skips `<fieldset disabled>` controls; `indeterminate` is cleared. VERIFIED on-device via inject: tap toggles both directions, repaints the checkmark, and the page onchange runs — full-page repaint [paint bytes 2060→430939]. `GoannaRenderPage::clickAt` INPUT branch.)*
- [x] Tapping XUL chrome content (`about:config`, `about:addons`) does NOT crash the daemon. *(2026-07-20: `SendMouseEvent` on XUL content SIGSEGVs the headless daemon [core dumped] — the XUL event/command frames assume a real widget the offscreen PuppetWidget lacks. Fix: SKIP the synthetic mouse events when the tapped content is XUL, detected from the DOCUMENT ROOT element's namespace [F-002 — NOT the hit element: ElementFromPoint can return null on a miss, and a XUL about: page embeds html:-namespaced nodes like about:addons's detail panes; either would slip a mouse event into the XUL frame tree and crash]. VERIFIED no-crash on-device for about:config [real XUL, skips] and about:addons. NOTE: about:addons currently renders a `<parsererror>` in our stripped libxul build — a separate engine-resource/branding-strip issue, not a crash.)*
- [x] Activating XUL buttons (e.g. `about:config`'s "I promise to be careful!" `oncommand`). *(2026-08-04: clicking `warningButton` swaps the deck — the following taps resolve to `<treechildren>` and the frame shows the real prefs table (Preference Name / Status / Type / Value). So the `oncommand` ran, not merely the click.)* ~~*(NOT DONE — firing a synthetic `command` event runs the XUL handler [ShowPrefs] whose tree/command frames hit the same offscreen fault and CRASH. Removed rather than crash the card; the button is left inert. Needs real headless XUL/widget support. See [[jihad-input-activation-and-tiling]].)*~~ ***STALE — struck 2026-08-10.** This note predates the criterion note beside it and was left attached to an `[x]` box, where it reads as an open item and sends a reader to re-fix working behaviour. It is superseded twice over: by the 2026-08-04 note above, and by R6, where the `about:config` warning button's `oncommand` demonstrably runs, the prefs tree replaces the deck, filtering works, and a boolean pref is toggled in both directions (proved from `profile/prefs.js`). The `[x]` is correct.*
**Dependencies:** cavekit-offscreen-rendering.md (R5)

### R2: Keyboard input
**Description:** Key commands produce correct DOM keyboard events and text entry.
**Acceptance Criteria:**
- [x] `keyDown`/`keyUp` with a webOS key code, modifiers, and character produce DOM key events with the correct key and modifier state.
- [x] Typing into a focused text field inserts the expected characters (letters, digits, symbols, and non-ASCII), at the caret, with no per-keystroke navigation flash.
- [x] `insertStringAtCursor` inserts text at the caret.
- [x] A visible text caret is shown for the focused editable, and typing/editing happens at it (not append-only).
- [~] Tapping an editable raises the VKB reliably on EVERY page in a session, and focusing a field does not scroll the page content off-screen. *(T4 (VKB stuck after multi-site session) FIXED 2026-07-18 (8ebca68): engine-driven editorFocused (Atlas IM-context port) drives the VKB from capture-phase focus/blur with clean false transitions to unwedge on. T1 (focus pushes the page up) addressed 2026-07-20 (e1b3197): setWindowSize now restores the pre-resize scroll on a VKB-shrink when the reflow pushed a focused field's content up, so a top-of-page search box stays put. A big contributor to the apparent VKB/tap breakage was the daemon CRASH (tick re-entrancy, see cavekit-navigation-events R6 / device-test-2026-07-19.md) — fixed 2be6d85. Remaining [~]: the white-band/"snap" jank on VKB toggle and a full on-device T1/T4 re-verification. See context/impl/device-test-2026-07-19.md.)* *(**2026-08-10 (T-133) — the white band is TRACED FROM SOURCE to a specific line, and the "942↔602 reflow fight" framing in device-test-2026-07-19.md is NOT where it comes from. NOTHING HERE IS DEVICE-VERIFIED: no device was available for this pass, and an instrument was landed instead of a fix.** The band is rows the ADAPTER never draws. `BrowserAdapter::handlePaint` maps the card's dst box onto the buffer with `sT = srcTop - renderedY + mScrollPos.y*inv`, and on the device (PGSurface) path it CLIPS the dst at whichever edge falls outside the buffer (`if (sT < 0) { dt += ceil(-sT/inv); sT = 0; }`, and the matching `sB > H` arm); on the dstBuffer path it writes `FILL = 0xffffffff` outright. Either way every row above `renderedY` or below `renderedY+renderedHeight` is left UNDRAWN, and an undrawn dst reads white on device — handlePaint's own comment records that as the earlier "VKB-whiteout". So the band is exactly `renderedY - mScrollPos.y` rows at the top, or `mScrollPos.y + winH - (renderedY+renderedHeight)` at the bottom. **The daemon's deferred geometry emit is NOT the cause** — it defers precisely to avoid that (`BrowserPageGoanna.cpp:376-378`).
      **What moves the pan out of the band is the ADAPTER, before the daemon can paint at the new height.** `handleWindowChange` sends `asyncCmdSetWindowSize` and then calls `mScroller->setViewportDimensions`, which zeroes the scroller velocity and calls `start()`; `constrain()` then spring-damps the pan against the NEW `bottomBoundary() = viewportH - contentH`. On a GROW (VKB DOWN, 602→942) that boundary rises by dh=340, so a bottom-of-page pan is out of bounds and animates UP over several 20 ms timer steps, each calling `BrowserAdapter::scrollTo` → `invalidate()` → a composite against the pre-resize buffer. **Quantified, and this is what "sometimes" is:** pre-resize at the page bottom, `lo = (C-602) - ovAbove` and the adapter clamps to `C-942`, so **gapTop = dh - ovAbove**, and `ovAbove` comes from the paint path's overscan DIRECTION BIAS — `h/8`=75 if the last paint was mid-scroll-DOWN (**265-row white band across the top**), `h/2`=301 if the page had settled (**39 rows**), `3h/2`=903 if the last paint was mid-scroll-UP (**no band at all**). It lasts the ~60-80 ms until the next full paint lands (≤16 ms tick + 40-66 ms render), i.e. 3-5 composite frames. A GROW also makes the dst dh taller than any band painted at the old height, which is the same defect on the BOTTOM edge wherever `hi` was capped by the content end or the 2048-row segment cap.
      **THREE CORRECTIONS to things that were being reasoned from.** (1) *`scrollCaretIntoViewAfterResize` is DEAD CODE in this port and can be no part of any VKB explanation.* Its first clause is `m_textCaretRect.x() != 0 || m_textCaretRect.y() != 0`; that rect is only ever written by `msgGetTextCaretBoundsResponse`, which never arrives, because `JihadBrowserServer::asyncCmdGetTextCaretBounds` is a `(void)proxy;` no-op (TODO T-016). So the adapter does NOTHING to keep a focused field visible across a VKB raise, and the whole job falls to the daemon's e1b3197 restore. (2) *That restore's comment described a guard the code does not contain* — it claimed the restore runs "only when the tapped field's top stays within the shrunken viewport", but the test is just `if (sy1 > sy0)`, so a field the VKB genuinely covers is pushed back under it. Not changed (e1b3197 was device-tuned and T-119 has not re-run it); the comment is corrected in place. (3) *`msgScrolledTo` is a THIRD producer of a mid-resize pan move* (assigns `mScrollPos` directly, `mScroller->scrollTo(..., animate=false)`) and re-clamps with `mContentHeight - mViewportHeight`, which the resize just changed; the daemon clamps its band with `mLastContentH * Zeff` instead, so the two sides can disagree by up to dh for one round trip, and since the band overlay is baked viewport-relative at the DAEMON's `bandY`, position:fixed content and the caret land at the wrong rows for those frames. Plausible second source of "snaps around", separate from the white band, and NOT measured.
      **THE INSTRUMENT, landed in the daemon so no `.ipk` and no adapter rebuild is needed.** `setScrollPosition` already computes `covered` against the last painted band; it now also prints, rate-limited to 10/s and only when rows are genuinely missing: `[jihad-bs] uncovered pan y=… h=… band=[lo,hi) gapTop=N gapBot=N sinceResize=Nms dh=N`. **`gapTop`/`gapBot` ARE the white band in rows**, at the instant the adapter asks for that pan; `sinceResize`/`dh` separate a VKB toggle from a fling that outran the overscan. ONE device run answers it: open a long page, scroll to the bottom, tap a field, dismiss the VKB. A line with `sinceResize` in the single-digit ms and `dh=+340` confirms the mechanism and hands over the band height; a large `sinceResize` refutes it and the cause is elsewhere. Read it beside `JIHAD_T1_LOG=1`'s existing `setWindowSize … scroll a,b -> c,d` line.
      **THE FIX IS ADAPTER-SIDE AND WAS DELIBERATELY NOT ATTEMPTED BLIND.** The gap frames are composited from a buffer published BEFORE the resize, so no daemon change can fill them, and the daemon's first post-resize band already covers the new pan (`lo = min(bandY,adY) - ovAbove`, both updated by then). The candidate is ~4 lines in each of handlePaint's two paths: when the source rect falls off an edge, SHIFT the source window instead of clipping the dst whenever the buffer has the rows, so the card holds position for ~60 ms rather than flashing white. That also changes fling behaviour (a sticky edge instead of the white one Atlas commit `aefab6d` deliberately chose), requires a full `.ipk` install, and cannot be verified without a device. Recorded, not shipped.)*
**Dependencies:** none

### R2a: Full editable navigation and editing
**Description:** The on-screen-keyboard editing keys drive the focused `<input>`/`<textarea>` correctly.
**Acceptance Criteria:**
- [x] Backspace deletes the code point before the caret; forward-Delete the one after; both are surrogate-pair aware.
- [~] Left/Right move the caret by a full code point; Home/End jump to the field start/end; Up/Down move by line in a `<textarea>` (and degrade to Home/End in a single-line input) without landing between a surrogate pair. *(**Downgraded 2026-08-10 under T-127, and the honest finding is that the Home/End half was probably never true on this device.** `EK_HOME`/`EK_END` were reachable ONLY from the Qt (`0x01000010`/`0x01000011`) and Apple/NSEvent (`0xF729`/`0xF72B`) codes, and this platform can deliver neither: the webOS key path is 16 bits wide so no `Qt::Key` fits (`Palm::WebView::keyEvent(unsigned short, …)` → `PlatformKeyboardEvent(unsigned short, …)` → `ldrh` into `rawkeyCode`), and `WebCore::keyIdentifierForPalmCode` in the shipped `libWebKitLuna.so` has no `0xF7xx` code and **no End code at all** — its only `Home` (`0xE0B1`) is the device Home button. Those two branches are deleted and `EK_HOME`/`EK_END` are now unreferenced. Why `[~]` and not `[ ]`: a single-line `<input>` still reaches field start/end, because `EditKey`'s `EK_UP`/`EK_DOWN` degrade to Home/End when the value has no newline (`GoannaRenderPage.cpp:1132-1140`) and Up/Down do arrive. There is no dedicated Home/End key on the VKB to press. Read from source and from the device binary, not re-measured — T-141 owns the device pass. Same pass must settle a second conflict: the binary's table reads `0xE0A0="Up" 0xE0A1="Down"`, the reverse of the `0xE0A0=Down 0xE0A1=Up` recorded in the 2026-07-15 entry below and in AGENTS.md (Left/Right agree). The code still uses the recorded pair, deliberately.)*
- [x] Typing or deleting while a range is selected replaces/removes the selection (e.g. an `onfocus=this.select()` search box).
- [x] Enter inserts a newline in a `<textarea>` and submits the form for a single-line `<input>`. Tab inserts a tab character in a `<textarea>` but moves focus to the next/prev text field in a single-line `<input>` (Shift+Tab goes back) — inserting a control char into a search/login value is wrong. *(2026-07-18: T2 root found + fixed. Submit went through DOMClick(), which MOZ_CRASHes from native code (SubjectPrincipal, no JSContext) — Enter SIGSEGV'd the daemon, read on-device as the stuck loading overlay. And SendMouseEvent fires the click event but NOT the submit default action in this embedding. Fix: FireFormSubmit — validate + cancelable `submit` event + form->Submit(), crash-safe. Enter + tapped submit buttons both navigate. focus_test D/E PASS on desktop; pending device retest.)*
- [x] Enter submission runs the form's constraint validation first: an INVALID form is not submitted and keeps focus + the keyboard so the user can correct it (never discards the edit target before knowing submission succeeded). *(2026-07 Codex F-290.)*
- [x] When a page focus/blur handler moves focus off the tapped/next field to a NON-text control (button/checkbox), the keystroke target is cleared rather than left on the old field — typing must never silently mutate a field that is no longer focused. Retargeting only follows focus to another text control. *(2026-07 Codex F-270/F-292.)*
- [x] After a programmatic value edit, a bubbling DOM `input` event is dispatched (from the guarded loop) so controlled/framework (React) inputs keep the edit instead of reverting it.
- [x] A held Backspace accelerates to whole-word deletion after it auto-repeats for a short interval.
- [x] `<input type=number>`/`email` etc. (no text-selection API) still accept typed characters.
- [x] Focus/blur handlers that navigate or submit run in the page-lifetime-protected pump/tick context, never synchronously in the YAP key callback (same crash class as R1).
- [x] No keystroke, character, or field value is ever written to the daemon log (F-163).
**Dependencies:** cavekit-offscreen-rendering.md (RENDER_CARET — a visible caret requires the offscreen render to draw it)

### R3: Touch and gesture input
**Description:** Touch and gesture commands map to DOM touch/zoom behavior.
**Acceptance Criteria:**
- [x] `touchEvent` (with the touches payload) produces DOM touch events with matching touch points. *(2026-08-04. Two things were wrong and both are fixed. **(1) Only the first point was ever parsed** — `touchEvent` read one `"x"`/`"y"` pair out of the payload, so a two-finger gesture was indistinguishable from a one-finger one; it now parses every object in the `[{"x":N,"y":N,"state":S}, …]` array the adapter builds, carries the whole list through the input queue, and dispatches it with one identifier per contact (identifiers are what let a page track each finger). **(2) W3C touch events were DISABLED**, so even a correct dispatch reached nothing: the engine default is 2 = autodetect and its own comment says autodetection works only on Windows and GTK3 — this is a cairo-headless build, so it settled on OFF, on a machine whose only input is a touchscreen. `dom.w3c_touch_events.enabled` is now 1 in the new shared `packaging/prefs/jihad-platform-prefs.js`. Measured: injecting `touch 0 400,600 460,620` gives the page `touchstart(n=2 0@400,600 1@460,620)` — exact coordinates, distinct identifiers.)*
- [~] A pinch gesture changes page zoom; a tap maps to a click. *(**2026-08-15 (T-120) — the fence is GONE and the ABI wall is down, but this box does not move, for the reason the 2026-08-10 note gives: neither pinch nor tap rides `doTouchEvent`. What is open here is still the human two-finger gesture, T-151.** Recorded here because this is where the T-120 evidence lives; the full account is `../impl/impl-touch-events.md`. **(1) HEADER RE-STAGED.** `adapter-deps/staging/include/webkit/npapi/npapi.h` now declares the 28-byte `NpPalmTouchEvent` (sha256 `462bcc89…1752b`/35552 B → `36b58a83…21c199`/36646 B, recorded in `build/webos-oe/adapter-deps.manifest` as origin `npapi-headers+jihad`). SURGICAL, not a whole-file swap: the debian copy also moves `npPalmApplicationIdentifier` from `NPNVariable` 2005 to `NPPVariable` 10002, and `BrowserAdapter.cpp` reads the app identifier through that enum on a device-verified path, so only the touch struct was taken across. Proof is the compiler's in BOTH directions — both adapter builds compile the `sizeof(NpPalmTouchEvent)==28` guard clean, and a deliberate rebuild against the old 12-byte header fails with `size '-1' of array 'jihadTouchAbiGuard' is negative`. The include path was not assumed: `-E -dI` shows the adapter's `<webkit/npapi/npapi.h>` resolving out of `staging/include`, which both `build-adapter-arm.sh` and `build-adapter-pdk.sh` list first. **(2) BLOCK ENABLED, DEFAULT OFF.** `doTouchEvent` is out of `#ifdef QT_FIXME` and uses `JihadTouchState`; `shouldPassTouchEvents()` gating is unchanged, so it is still user-scalable=no pages only. The switch is `JIHAD_TOUCH_EVENTS=1` in LunaSysMgr's environment (env because the adapter already reads `LOG_BROWSER_ALERTS` that way and the value must be decidable in the constructor); one `LOG_WARNING` line when on. Off by default because this is new capability stock never shipped, it has never run on hardware, and it is the double-activation class. Two things were found while enabling: the TouchEnd loop was bounded by `changedTouches.length` while indexing `touches.points` — reading past the end of the shorter array, which is the actual source of the "7,000,000" coordinates the code apologised for — and NOTHING had ever set `npPalmEnableTouchEvents`, without which WebKit sends a plugin no touch events at all and un-fencing the block would have been a silent no-op. **(3) SUPPRESSOR, DAEMON-SIDE.** `GoannaRenderPage::TouchEvent` now returns `sendTouchEventToWindow`'s `preventDefault` (and `false` on a failed dispatch — a failed dispatch is not a consumed gesture). `BrowserPageGoanna::touchSuppressesMouse` consumes it. CORRELATION RULE, since the pen and touch streams share no id, no sequence number and no timestamp — only the input queue, hence a total ORDER: (a) sequence state is primary — from a consumed touchstart until its touchend, every queued down/up/move/click/contextmenu is dropped, no geometry involved; (b) a 400 ms + 48 document-px window after the touchend catches the tail (the pen-up and synthesized click land just after it); (c) a 5000 ms cap disarms rule (a) if a touchend never comes, because otherwise a lost cancel suppresses all pointer input for the life of the page. Comparison is on document coordinates, before the pump's `docToViewport`. Needs no switch of its own: it only arms when a touch command arrives. FAILURE MODES, unmeasured because there is no device: a page that consumes touchmove but not touchstart is not suppressed (matches the W3C compatibility rule, and equals today's behaviour); a second finger within 400 ms and 48 px of a consumed gesture's end loses its click; and **if the device delivers pen-down BEFORE touchstart, one leading `mousedown` escapes** — the mouseup/click still do not, so no click is fabricated, but establishing the real order needs hardware. **(4) NOT VERIFIED ON HARDWARE. No device existed this session.** Built and packaged only: ARM daemon (check-yap-contract PASS, 73 commands / 58 messages), both adapters (crosstool AND PDK — the `.ipk` ships PDK), all three `.ipk`s, `libasound.so.2` confirmed present in each. The device test procedure is written down at the end of `../impl/impl-touch-events.md`; step 6 (remove the `preventDefault` and confirm the mouse events come BACK) is the one that catches a suppressor that never disarms.)* *(**2026-08-04 — a blocker found while closing the other two criteria: the ADAPTER never forwards touch at all.** `BrowserAdapter::doTouchEvent`'s entire body is inside `#ifdef QT_FIXME`, so no `touchEvent` command has ever left a card, and no page on the device has ever received a touch event regardless of how correct the daemon side is. Turning it on is not a one-line change: the disabled code builds a `Palm::TouchPointPalm::State` for each point, and that header does not exist in this build (it came from webkit-supplemental) — the state values would have to be re-declared, and since the ONLY consumer is our own daemon, which ignores the field, that is safe to do but must be documented as ours rather than passed off as the frozen contract's. It also needs care: touch and mouse both reaching the page is the double-activation class this project treats as critical, and `shouldPassTouchEvents()` gates it to `m_passInputEvents` or a non-user-scalable viewport. Deliberately NOT done as a drive-by. **2026-08-10 (T-120) — that 2026-08-04 note is right that the fence is real, WRONG about which criterion it blocks, and it understates the cost by an order of magnitude.** (a) MIS-ATTRIBUTION. Neither half of THIS criterion runs through `doTouchEvent`. Pinch-to-zoom rides `handleGesture`, and with `m_passInputEvents` false — nothing passes the `usemouseevents` plugin param in `app/`, `app-mochi/` or `app-mojo/`, grepped — it takes the adapter-side `doGestureStart/Change/End` branch, which is the zoom path already device-verified 2026-07-27; tap-to-click rides `handlePenClick`/`clickAt` and is R1, closed. The fence blocks DOM TouchEvent delivery to CONTENT, i.e. the FIRST criterion of this R3, not pinch and not tap. What actually holds this box open is the human two-finger gesture (T-151, BLOCKED-EXTERNAL). (b) THE FENCE IS AN ABI WALL, not a re-declaration chore. Measured on the build host, NO DEVICE: the device pushes a 28-byte `NpPalmTouchEvent` (`NpPalmTouchList touches/targetTouches/changedTouches` at +0/+8/+16, `modifiers` at +24) while the `npapi.h` the adapter compiles against declares a 12-byte `{int32_t touchCount; NpPalmTouchPoint* touches; int32_t modifiers}`. Evidence: `WebCore::PluginView::handleTouchEvent` at 0x4e913c in the device's own `build/webos-oe/device-binaries/libWebKitLuna.so` builds THREE variable-length point arrays from `TouchEvent`+0x6c/+0x70/+0x74 and stores three (points,length) pairs at NPEvent data +0/+8/+16 before `dispatchNPEvent`; the very checkout that supplied our header carries the 28-byte declaration in its debian INSTALL copy (`adapter-deps/npapi-headers/debian/npapiheaders/usr/include/npapi.h`) while the repo-root copy in the same checkout has the 12-byte one, and both isis (`../ref-BrowserAdapter`) and Atlas read `event->changedTouches`. So enabling the block against the header we build with does not misplace a touch — it reads a length as a pointer and dereferences it INSIDE LunaSysMgr. The bad header lives in a gitignored build-input dir, so the remedy is a re-stage, not a source edit; a `sizeof(NpPalmTouchEvent) == 28` compile-time guard now sits inside the fence so the mistake fails the build instead of the shell. (c) The state values are declared as OURS (`JihadTouchState` in `BrowserAdapter.cpp`, ordering taken from `WebCore::PlatformTouchPoint::State`, which the missing `Palm::TouchPointPalm` mirrored) and are documented as NOT the frozen contract. Nothing consumes them: `BrowserPageGoanna::touchEvent` parses only `"x"` and `"y"`. (d) THE DAEMON END IS NOT A STUB. `asyncCmdTouchEvent` -> `BrowserPageGoanna::touchEvent` -> input queue -> `GoannaRenderPage::TouchEvent` -> `nsIDOMWindowUtils::sendTouchEventToWindow`, exercised by the inject `touch` command. One real defect found there and fixed in one line: type 3 (cancelled) fell into the else and became a phantom `touchmove` at the released coordinates; it now ends the sequence. A true `touchcancel` needs a new `PM_` value — the engine already accepts the string. (e) DOUBLE ACTIVATION IS UNSOLVED, AND THE HOOK ALREADY EXISTS AND IS THROWN AWAY. A finger already produces pen events -> `asyncCmdMouseEvent`/`clickAt`, so touch would double-fire, and Gecko's own suppression cannot apply because our mouse events do not originate from touch — they arrive on a separate YAP command. `GoannaRenderPage::TouchEvent` discards the preventDefault return of `sendTouchEventToWindow`, which is exactly the "page consumed this gesture" signal a suppressor needs. (f) DEPLOYMENT COST, because none of this rides `push-engine-update.sh`: the adapter is built by BOTH `build-adapter-pdk.sh` (the build the `.ipk` ships) and `build-adapter-arm.sh`, and reaches the device only as an `.ipk` install or a hand-swap of `/usr/lib/jihad/<variant>/BrowserAdapterImpl.so` plus `/usr/lib/BrowserPlugins/BrowserAdapterJihad*.so`, followed by a LunaSysMgr restart. (g) Stock never forwarded touch either: isis AND Atlas both ship `doTouchEvent` under the same `#ifdef QT_FIXME`, so this is NEW capability, not restored parity. The rest of the original note still stands:* 2026-07-31 reconciliation — partial credit, box stays OPEN: the ZOOM half of the chain is device-verified as of 2026-07-27 (`../impl/zoom-fix-2026-07-27.md`, commit 8d7865c — a zoom command magnifies + pans on device, verified through the daemon inject channel + frame.ppm), and tap→click is verified (R1). What is NOT verified is the gesture path itself: that doc records "the end-to-end on-screen feel (adapter driving pinch → daemon magnify → adapter composite at inv==1) is a human/gesture check (no gesture injection into LunaSysMgr)". So a real two-finger pinch has never been recorded as changing zoom on device.)*
- [x] Multi-touch point count and coordinates are preserved. *(2026-08-04, same run: `n=2` with both points' coordinates exact through start and move, and `touchend` correctly reports `n=0` because `TouchEvent.touches` lists the REMAINING contacts. One upstream behaviour to expect rather than debug: Gecko dispatches one DOM event per CHANGED touch (`PresShell::DispatchTouchEventToDOM` loops over `mTouches`), each carrying the full list — so a two-finger touchstart produces two `touchstart` events with `touches.length == 2`, not one. Verified against the engine source and by point count: one contact gives one event, two give two.)*
**Dependencies:** cavekit-offscreen-rendering.md (R5)

### R4: Drag and flick scrolling
**Description:** Drag commands scroll content and overflow regions.
**Acceptance Criteria:**
- [x] `dragStart`/`dragProcess`/`dragEnd` scroll the page and CSS-overflow elements.
- [x] Scrolling emits scrolled-to updates consistent with the rendered output.
**Dependencies:** cavekit-offscreen-rendering.md (R4)

### R5: Coordinate mapping under zoom/scroll
**Description:** Content coordinates resolve to the correct page location regardless of transform.
**Acceptance Criteria:**
- [x] With the page zoomed and scrolled, a click at a given content coordinate hits the element actually shown at that location. *(REPORTED 2026-07-15: a tap on example.com's sole link hit-tested to `<HTML>` (missed). ROOT (2026-07-27): the adapter-vs-daemon zoom DID disagree — the daemon rendered a near-1 fit-zoom at 1× but advertised `contentZoom=mZoom` (~1.005), so the adapter's composite scale and its screen→content click divisor were off by ~(mZoom−1), a few-px vertical offset that made small targets miss. FIXED alongside the zoom rework: the daemon now reports the EFFECTIVE render scale, and the new document-relative zoom keeps the render, display, and click coords consistent. Verified on-device — a tap on example.com's "Learn more" link hit-tests the anchor and navigates. See context/impl/zoom-fix-2026-07-27.md; the on-device link-tap confirmation itself is recorded in commit **d4f0842** ("Verified on-device: a tap on example.com's 'Learn more' link now hit-tests the anchor and navigates (contentZoom=1.0000)"), not in that impl doc.)*
- [x] Mapping is consistent between input and the geometry the renderer reports.
**Dependencies:** cavekit-offscreen-rendering.md (R5)

### R6: XUL/chrome documents accept input like any other page
**Description:** Input works on XUL documents — `about:config`, `about:addons`, and every other chrome-privileged page — not just HTML content. User requirement, 2026-08-01: *"fix xul input so it works everywhere like about:config etc"*.

**Current state (the thing to fix):** synthesized input on XUL **crashes the daemon**. `nsIDOMWindowUtils::SendMouseEvent` on XUL content SIGSEGVs — the XUL event/command frames assume a real widget that `PuppetWidget` does not provide — and firing a synthetic `command` event on a XUL button crashes the same way (verified on-device 2026-07-20: `about:config`'s "I promise to be careful!" button re-entered the fault through `ShowPrefs`' XUL tree). The shipped mitigation is a **crash-avoidance skip**, not a fix: `GoannaRenderPage.cpp:1600-1645` detects the XUL namespace (`http://www.mozilla.org/keymaster/gatekeeper/there.is.only.xul`) and drops both `SendMouseEvent` calls, so XUL pages render and no longer crash on tap — and are completely inert. See [[jihad-input-activation-and-tiling]].

**Acceptance Criteria:**
- [x] The root cause of the XUL SIGSEGV is identified and named (which widget/frame path dereferences what `PuppetWidget` leaves null), not worked around. The existing `isXul` skip is REMOVED as part of this, not left in place behind a fixed path. *(2026-08-04. The `isXul` skip is gone from the tree, and the crash does not reproduce (25 scattered taps, criterion below). Two named defects sat underneath, and neither was a null deref — the honest finding is that the SIGSEGV attribution was WRONG and the real fault was that events went nowhere at all: **(1) `PuppetWidget::GetCurrentWidgetListener` consults only `mAttachedWidgetListener`**, never falling back to `mWidgetListener` the way the real widget does (`widget/gtk/nsWindow.cpp` `GetListener`: `mAttachedWidgetListener ? mAttachedWidgetListener : mWidgetListener`). PuppetWidget was written for the content-process case, where the view is always attached; in this standalone embedding nothing attaches, so everything routed through `DispatchEvent` was silently DROPPED. Mouse input hid it, because `nsIDOMWindowUtils` has `SendMouseEventToWindow` which reaches the presShell directly — there is no `SendKeyEventToWindow`. Fixed in patch 0005. **(2) No `keypress` was ever synthesized.** Gecko does not derive keypress from keydown for synthesized events, so only `keydown`/`keyup` were sent — and XUL `<key>` elements match on keypress. `BrowserPageGoanna::keyDown` now sends the keypress too.)*
- [x] `SendMouseEvent` on a XUL document does not crash the daemon, under repeated taps across a full session. *(2026-08-04: 25 taps scattered across `about:config` — its warning deck, the prefs tree, headers and scrollbars — with the daemon alive at the end. That is the crash the 2026-07-20 skip was added for; it does not reproduce.)*
- [x] `about:config` is operable end to end: the warning button activates, the prefs list renders, a pref can be filtered, selected, and its value changed — through the adapter's normal synthesized input path. *(2026-08-04 — ALL of it now, including the back half that no test had covered. The warning button's `oncommand` runs, the tree replaces the deck, typing filters the list, and a double-click on the filtered row TOGGLES the boolean. Proved from `profile/prefs.js` after a clean shutdown — the engine's own persisted state, not the UI that made the change — and proved in BOTH directions: `extensions.update.enabled` false→true wrote `user_pref(…, true)`, and true→false removed the line entirely (Gecko does not persist a value equal to the default). Reaching the row needed a new `clickoff <dx> <dy> <count> <selector>` inject command: a XUL tree is view-backed, its rows are not DOM nodes, so no selector reaches one and a centre click on a filtered tree lands below every row.)* *(2026-08-01, F-6 — the FIRST THREE clauses are measured on desktop (`xul_test` C/D/E, default click coordinate): the warning button's `oncommand` runs, the prefs tree replaces the deck, and typing filters the list. The back half — selecting a tree row and CHANGING a pref value — is not exercised by any test, so it stays open, and "about:config is fully operable" was an overclaim wherever it was written. Nothing here has run on the device since the change.)*
- [x] `about:addons` is operable: list items select, and enable/disable/remove buttons activate (this is what cavekit-addons-extensions.md R2 depends on).
- [x] XUL default actions actually run — matching the HTML lesson recorded in R1, where a synthesized click fires `onclick` but the engine skips the DEFAULT action in this embedding, so each native control needed hand-activation. Establish whether XUL needs the same treatment and say so explicitly rather than assuming the click suffices.
- [x] Keyboard input reaches XUL documents (text entry into `about:config`'s filter box), not only mouse. *(2026-08-04 — REAL key events now, not just the editor path. The decisive test isolates XUL from everything else: with the `about:addons` tools menu open (`popups=1`), an **Escape** key rolls it up (`popups=0`). That is the XUL popup manager's own key handling — no DOM value to write, no editor involved, and nothing a synthesized mouse event could produce. On content, a document-level listener now sees `down:65,press:65,up:65` where it previously saw nothing at all. The two causes are named in the first criterion; both needed the engine fix (patch 0005), so this cost the libxul rebuild that earlier notes had flagged as a deliberate decision. The `popups` inject command was added as the readback, since an open menu changes no DOM a selector can observe.)* *(2026-08-01, F-6 — PARTIAL, and the half that works is not the half this AC asks for. What is measured (`xul_test` phase E): a tap on the XUL `<textbox>` is adopted as the edit target via the focus manager (which resolves through the XBL-anonymous `html:input`), and `insertStringAtCursor` → `InsertText` puts text in through the ENGINE EDITOR (SetValue/SetSelectionRange, the same path HTML fields use), which filters the prefs list. What does NOT work: real DOM key events. `GoannaRenderPage::KeyEvent` calls `nsIDOMWindowUtils::SendKeyEvent`, which has **no `…ToWindow` variant** — it goes straight to `widget->DispatchEvent` and the un-attached `PuppetWidget` drops it, exactly as before T-067. So a XUL widget that reacts to keydown/keypress rather than to its field's value (tree type-ahead, `<key>` elements, Escape/Enter handling) still gets nothing. The fallback patch to `PuppetWidget::DispatchEvent` (fall back to `mWidgetListener` when `mAttachedWidgetListener` is null, the way `nsWindow::DispatchEvent` does) that earlier notes described as the fix **does not exist in the tree** — `build/desktop/patches/` has 0001–0010 and none of them touch PuppetWidget. It is ~2 lines and would also fix `SendKeyEvent` and every other widget-routed synthesis at the source, but it costs a full libxul rebuild (desktop + ARM) and re-bundling, so it is a deliberate, flagged decision rather than a drive-by.)*
- [x] No regression to HTML input: the R1/R4/R5 device evidence (checkbox/radio activation, drag scrolling, zoomed link hit-testing) still holds. *(2026-08-04, re-run on desktop after the engine change, since patch 0005 touches the path EVERY event takes: a checkbox click fires `change` with `checked=true`, a link click fires its handler, and typing into a text field still puts the text in (`VAL[Hi]`). The engine fix only ADDS a fallback where the attached listener is null, so the attached path — the one the content-process case uses — is untouched.)*
**Dependencies:** R1, cavekit-offscreen-rendering.md (the offscreen widget is the thing lacking what XUL expects)

### R7: Keyboard arbitration between a focused plugin and the card chrome

**Description:** A keystroke on this device has TWO possible consumers, and the adapter decides. The
key is always forwarded to the daemon, so the engine and any plugin see it either way; the adapter's
return value decides whether the CARD ALSO acts on it. That single boolean is the difference between
typing into a Flash game and typing into the address bar at the same time
(cavekit-addons-extensions.md R7).

The test it used — "is an HTML editor focused" — cannot describe a plugin. **A Flash game focuses no
editor**, so the flag is false for the whole session and every arrow key or WASD press also drove the
chrome. The mirror-image bug exists too: a page that auto-focuses an input on load leaves the flag
true forever, so after the user taps the address bar the adapter keeps eating every keystroke.

**Acceptance Criteria:**
- [x] Keys are only swallowed for content when the content actually has focus. *(2026-08-10 —
      `handleKeyDown`/`handleKeyUp` now gate on `mPageFocused` as well as `bEditorFocused`. Ported
      from Atlas BrowserAdapter commit `ce199bc` (Herman van Hazendonk, Apache-2.0); recorded in
      NOTICE. Fixes the reported "can't type an address after a site loaded".)*
- [~] Keys are swallowed for content when a PLUGIN has focus, even though no editor does.
      *(2026-08-10 — implemented by adding `mFlashGestureLock` to the same expression. That latch is
      already the adapter's notion of plugin engagement: set when a tap lands inside a plugin rect,
      cleared by a tap outside one, and it is the same latch that decides whether a DRAG goes to the
      plugin instead of panning the card — so keyboard and pointer focus stay consistent by
      construction rather than by a second, separately-drifting flag. **Adapter-side, so it needs a
      full `.ipk` install to take effect; NOT yet device-verified.**)*
      *(**CORRECTION 2026-08-15 (T-107 source read) — the sentence above about how the latch is set
      is WRONG, and the error changes what "plugin has focus" means for this whole requirement.**
      Only two sites ever set `mFlashGestureLock` true: `BrowserAdapter::handlePenDoubleClick`
      (`render/adapter/BrowserAdapter.cpp:1583`) — a **DOUBLE-tap** inside a plugin rect — and
      `js_smartZoom` (`:3119`), which has ZERO in-tree callers across all three shells.
      `handlePenDown` (`:1432`) only ever CLEARS it (other clear sites `:1576`, `:1868`, `:1993`,
      `:3113`). So a user single-tapping a Flash game gets NO keyboard lock: engaging it takes a
      double-tap, which also smart-zooms (`:1586` falls through to `prvSmartZoom`). Whether
      double-tap-to-engage is acceptable UX or the latch needs a single-tap set is a DESIGN
      decision that should be made deliberately, not inherited from this misreading. Also: no latch
      log exists in the shipped adapter — `render/adapter/Debug.h:31` has `DEBUG` commented out, so
      every `TRACEF` is `(void)0`; T-124's "prove the runtime value by adapter-side log" needs
      either DEBUG re-enabled or a `g_message` added, plus an `.ipk` reinstall, before any run can
      show it. The full device procedure (address-bar screenshot observable, wrong-card guard,
      per-run delivery counter, `bEditorFocused` confound control) lives on the keyboard criterion
      of cavekit-addons-extensions.md R7, 2026-08-15 annotation.)*
      *(**2026-08-10 (T-106) — the gate IS on the device, established by PROVENANCE, and a
      cheaper-looking test that would have "disproved" it is recorded here so nobody repeats it.**
      The device's `/usr/lib/jihad/enyo/BrowserAdapterImpl.so` has md5
      `0410a348042af58a7e711b495db69833`, which is the PDK build of this source — the same md5 this
      handoff records for the shipped adapter — so the binary in place was built from the tree that
      contains the gate (`render/adapter/BrowserAdapter.cpp:1404, 1465, 1507`).*
      ***The INVALID test, which returned a confident zero:*** *`strings BrowserAdapterImpl.so |
      grep -c FlashGestureLock` → **0**, on both adapter binaries. That is not evidence of absence
      and could never have been: `mFlashGestureLock` is a private C++ member, it appears in zero
      log or format strings (checked — 6 uses, none in a printf), and member names do not survive
      into a stripped `.so`. Reading that 0 as "the gate is missing" would have sent someone to
      re-implement working code. **Provenance (md5 against a known build) is the check; a `strings`
      grep for a symbol name is not.***
      *What is still genuinely unverified is the RUNTIME value — that `mFlashGestureLock` is true
      throughout Flash play — which is the next criterion and needs adapter-side logging, not a
      binary inspection.)*
- [~] Verified on device with an interactive plugin: keys drive the plugin and do NOT leak to the
      chrome while the plugin has focus, and DO reach the chrome again after a tap outside it.
      *(2026-08-10 — the PLUGIN half is proven: `render/goanna/test/make-key-swf.py` reacts to a
      keystroke in its own AVM1 (green 11768 -> red 11750 across one `key 65`). The chrome half —
      that the card does not ALSO act on it — is card-side and still unverified. Superseded note:
      the old blocker below is resolved.)* *(was: Blocked on a test asset: `make-anim-swf.py`/`make-audio-swf.py` emit no AVM1 bytecode, so
      there is no keyboard-driven SWF yet. The in-tree control plugin logs key events and can prove
      delivery, but not the arbitration, which is a card-side behaviour.)*
- [ ] The virtual keyboard can be raised deliberately for a plugin that wants text, given that the
      VKB is driven by `msgEditorFocused` and a plugin never sets it.

**Dependencies:** R2, cavekit-addons-extensions.md (R7), cavekit-ipc-contract.md
**Note:** the adapter half of this is the frozen-contract client (`render/adapter/`), so changes here
are NOT covered by `push-engine-update.sh` — they require rebuilding and installing the `.ipk`.

## Out of Scope
- Producing frames (cavekit-offscreen-rendering.md).
- IME/virtual-keyboard UI (webOS sysmgr concern; only the resulting key/text commands are handled here).
- Native XUL **popups/menus** as OS-level windows. If a popup requires a real toplevel widget the headless
  path cannot provide, an in-content fallback is acceptable — but that is a finding to record, not a
  silent omission.

## Cross-References
- See also: cavekit-offscreen-rendering.md, cavekit-navigation-events.md

## Changelog
- 2026-06-30: Initial draft.
- 2026-07-04: Reconciled — R1 click/hold, R4 drag-scroll, R5 coord-mapping-under-zoom all verified (INPUT/INPUT2/COORDMAP PASS); R2 keyDown/keyUp verified. Pending on-device: R2 field typing + insertStringAtCursor, all of R3 touch/pinch/tap (offscreen widget does not route synth touch on desktop).
- 2026-07-15: R2 field typing DONE + verified on-device — the engine editor works headless after UXP patch 0010, so typing runs through the real engine caret (SetValue + SetSelectionRange), not the old append-only content-attribute path. Added R2a (full editable navigation/editing): arrows, Home/End, Up/Down, forward-Delete, selection-replace, Enter (newline in textarea / submit in input, deferred to pump per R1's crash rule), Tab-inserts-tab, held-Backspace word acceleration, `<input type=number>` fallback, surrogate-pair safety — all verified on-device via the VKB, reviewed by codex gpt-5.6-sol (F-219..F-225) then the cavekit inspector while codex is rate-limited. Visible caret required a UXP change (patch 0011: offscreen RenderDocument must pass `nsIPresShell::RENDER_CARET`). webOS VKB arrow keycodes measured on-device: Down=0xE0A0, Up=0xE0A1, Left=0xE0A2, Right=0xE0A3 (a private-use block, not Apple/Qt ranges). R5 has a REPORTED link-hit-test miss on real sites (see R5) — open.
- 2026-07-17: R2a hardened (Codex gpt-5.6-sol F-264..F-292): Enter submission is constraint-validation-gated (invalid form keeps focus + VKB, F-290); the keystroke target is cleared when a focus handler moves focus to a non-text control and only follows focus to another text control (F-270/F-292); the pending `input` event flushes before a queued tap so a typed edit is observed before a submit/link click (F-291); the deferred `input` event targets the actually-edited element (F-266/F-267).
- 2026-07-18: R2 VKB is now ENGINE-DRIVEN (Atlas IM-context port, device T4): capture-phase focus/blur on the top document merges into the VKB state each pump tick — script-driven focus moves/blurs raise/lower the keyboard without a tap, blur gives the app clean false transitions to unwedge on, and a load-time autofocus cannot grab the VKB before the first tap (Atlas gate). Typing retargets to the engine's focused field. Tap path unchanged (always-re-raise). Pending on-device retest.
- 2026-07-20: R1 native-control activation. Synthesized clicks in this embedding fire `onclick` but not the click DEFAULT ACTION, so tapped checkboxes/radios never changed. Now hand-flipped + a `change` event dispatched (VERIFIED on-device, daemon `b4d1a01`). Also fixed a daemon SIGSEGV when tapping XUL `about:` pages (`SendMouseEvent` on XUL crashes → now skipped via namespace check). XUL button activation (synthetic `command`) also crashes → left inert, deferred. See [[jihad-input-activation-and-tiling]] and context/impl/impl-overview.md.
- 2026-07-27: R5 coordinate-mapping RESOLVED (commit c342009 = the kit edit; fix in d4f0842). The 2026-07-15 REPORTED link-hit-test miss was the daemon advertising `contentZoom=mZoom` (~1.005) while rendering a near-1 fit-zoom at 1×, desyncing the adapter's composite scale from its screen→content click divisor; the daemon now reports the EFFECTIVE render scale. Verified on-device (d4f0842). The 2026-07-15 entry above still calls R5 open — historical, superseded here.
- 2026-07-31: Reconciliation against recorded evidence (no box changed state). R5's note now names commit d4f0842 as the source of the on-device link-tap confirmation (`../impl/zoom-fix-2026-07-27.md` documents the zoom rework but does not itself record that tap). R3's pinch AC gained a note separating what IS device-verified (the daemon-side zoom magnify/pan, 2026-07-27) from what is not (a real two-finger gesture — no gesture injection into LunaSysMgr, human check per `../impl/zoom-fix-2026-07-27.md`); it stays open. Frontmatter `last_edited` was stale at 2026-07-15 despite the 07-20 and 07-27 edits.
- 2026-08-01: Adversarial review of the T-067/T-068 XUL-input change (`../impl/impl-review-findings-xulinput.md`) — eight findings, seven fixed in code, one (F-6) recorded here. **F-1 (P0):** one tap delivered TWO click sequences on every `shouldPassInputEvents()` page, because the adapter's pen path (`asyncCmdMouseEvent` down/up) and Jihad's single-tap `asyncCmdClickAt` BOTH act now that events reach the DOM — a checkbox toggled twice (net zero, two `change` events), forms double-submitted, link taps raced two loads. Fixed in the DAEMON (`GoannaRenderPage::MouseEvent` records the pen down/up pair; `ClickAt` stands down when it matches), because that keeps clickAt's hit-test/VKB/near-miss work on those pages and — unlike an adapter-side gate — is provable on the desktop harness. **F-2/F-3:** the checkbox and submit fallbacks fired exactly when the page DECLINED (UXP reverts its pre-toggle on `eConsumeNoDefault`, so a `preventDefault`ed click is indistinguishable from a dropped one by state); both now require DELIVERY DETECTION — a one-shot capture-phase `click` listener on the tapped control — and the submit classifier finally asks `GetDisabled`. **F-7:** post-click focus adoption now requires that the tap caused the focus (changed, or the focused control is inside the tapped element), so a cancelled `mousedown` cannot re-raise the VKB over a password field the user tapped away from. **F-8:** the chrome-JS console bridge no longer passes `about:` wholesale or every empty source (`about:blank` is content; so is an eval with no source name) — F-163. **F-5:** the R6 harness could not fail (`rc` never assigned; the runner's last command was an `echo`) — every phase now gates, and the failure path is DEMONSTRATED three ways (see the file header). Two new phases: K (one tap = one click sequence, which caught F-1 before the fix) and L (a declined click must not be re-run). **F-4:** the code comments no longer imply the 2026-07-20 SIGSEGV is explained — the dropped-event defect is fixed; that crash is unattributed, and none of this has run on the device.
