---
created: "2026-06-30"
last_edited: "2026-08-15"
---

# Cavekit: IPC Contract Preservation

## Scope
The engine-agnostic render daemon and its contract with the BrowserAdapter: the
YAP command/message interface, the shared-memory framebuffer protocol, the
daemon lifecycle/page manager, and the LunaService surface. The **command/message
interface is frozen**: the port hinges on the YAP commands and messages staying
byte-identical so the UI needs no change. The self-contained build does rename the
adapter's NPAPI MIME and YAP *server socket name* for coexistence with the stock
browser (see R5, `jihad-self-contained-arch.md`) — a transport-addressing rebrand
that leaves every command/message signature untouched. Reference:
`docs/IPC-CONTRACT.md`, `context/refs/refs-overview.md`.

## Requirements

### R1: YAP command/message interface is byte-identical to upstream
**Description:** The set and signatures of YAP commands (async + sync) and messages match the isis-project BrowserServer exactly.
**Acceptance Criteria:**
- [x] The generated YAP interface (commands and messages, including argument types and order) is unchanged from upstream `BrowserServerBase.h`.
- [x] **No existing command or message is removed, renamed, or has its signature altered, and nothing is added except a NEW id that is optional in both directions and documented.** *(**Wording amended 2026-08-10, deliberately and with the reason recorded.** It previously read "No command or message is added, removed, renamed, or has its signature altered", which the shipped contract no longer satisfies — and leaving a criterion permanently false is worse than either fixing the code or fixing the criterion. The user authorised the single addition on 2026-08-10 to fix the Flash frame rate, so the code is correct and the criterion was the thing out of date. The amendment deliberately keeps the part that protects the port — nothing existing may change — and narrows the new permission to additive-and-optional-and-documented, which is exactly what makes 0x1600 safe. It is NOT a licence to grow the contract: any further addition needs the same three properties and its own authorisation. The compatibility argument is below and the wire detail is in docs/IPC-CONTRACT.md.)* *(2026-08-10 — **ONE command was ADDED**, and the box is honest about it rather than quietly still ticked. `asyncCmdSetExtraBuffer` (0x1600, two int32s) announces a THIRD shared-memory buffer. Nothing existing changed: every stock id, argument list and byte order is untouched, and `connect`/`thaw` — which carry exactly two keys and could not express a third — were not widened. The addition is optional in BOTH directions, which is what makes it safe against a contract otherwise held byte-identical: an adapter that never sends it leaves the daemon on two buffers (`mKey3` stays 0 and every path behaves as before), and a daemon that does not implement it hits `BrowserServerBase`'s `default:` arm, logs `Unknown async cmd` and continues — the server-side declaration is a non-pure virtual with an empty body for exactly that reason. Old-with-new works in either combination; only two new halves engage the third buffer, and both ship in one `.ipk`. Rationale and measurements in docs/IPC-CONTRACT.md; the user authorised the change on 2026-08-10 to fix Flash frame rate. Measured: `deferred` 77 -> 0, `wanted` == `done`, composite 27.1-32.2 fps, frame-gap max 127 ms -> 52-57 ms.)*
- [x] If regenerated, it is regenerated from the in-tree `.defs` definition with the in-tree generator, not hand-edited. *(**Wording amended 2026-08-15, with the reason recorded.** It read "from the upstream `.yap` definition"; the 2026-08-10 note below had already established that the upstream definition is the one input that must NEVER be used, so the criterion as written instructed the reader to break the wire. The amendment keeps the whole force of the criterion — regeneration, not hand editing — and only names the correct source, which now lives in this repo alongside the generator that consumes it.)* *(**2026-08-15 — CLOSED, `[~]` -> `[x]`.** The one hand edit named below is gone, and the way it went is the one the note itself prescribed: **the generator is now vendored and patched**, at `render/browserserver/CodeGen/YapCodeGen.cpp` with a `README.md` beside it. Three changes: `ignoreLine()` advances its pointer, so the leading-space HANG recorded below as a live trap is fixed (upstream still hangs; measured side by side, upstream times out at 10 s while the vendored copy parses the same file and exits 0); the type field takes flags; and an `optional` async command is emitted server-side as a NON-pure virtual with an empty default body instead of `= 0;`. `0x1600` is marked `async optional` in the in-tree `.defs`, so **the default body that AC2's "optional in both directions" rests on is now GENERATED**, not re-applied by hand. Measured, host only (Qt 5.15.11 / g++ 14.2.1), no device operation and no ARM build: generated `BrowserServerBase.cpp` differs from the shipped file by **exactly the four cosmetic diffs already recorded and nothing structural**; generated `BrowserServerBase.h` reproduces the 0x1600 declaration and its body **byte-for-byte**; `check-yap-contract.sh` run against the fully generated four-file set now reports **ZERO defects**, where the best previously possible was exactly one, and it still passes on the tree as it ships. The patches are also proved inert for everything else: both generators were built and run over the UPSTREAM `.defs`, and all four outputs are byte-identical. **What is honestly NOT byte-reproducible, and why it does not reopen this box:** three cosmetic diffs remain in the server `.h` — the copyright year, the generically worded comment above the optional declaration, and `asyncCmdSetExtraBuffer` being DECLARED one position earlier. That last cannot be closed by any `.defs`, because the shipped `.h` and the shipped `.cpp` disagree with each other about where 0x1600 sits (each was hand-edited separately) while one `.defs` ordering drives both; the generated pair is self-consistent, and declaration order within one class in one binary has no wire effect. None of the three is a hand edit anyone must re-apply, which is what this criterion asks. The generated files were deliberately NOT copied over `render/browserserver/Src/` — the task was to prove reproduction, not to rewrite live code while another agent held the ARM objdir. Full record, including the client pair's older-generator formatting divergence: `context/impl/impl-yap-codegen.md`.)* *(**2026-08-10 — this criterion's own premise was WRONG, and the correction is the finding.** The generator IS present (`../ref-BrowserServer/CodeGen/YapCodeGen.cpp`); it was built here against Qt 5.15 and RUN, so regeneration is host-testable. Regenerating from the upstream `.defs` does not reproduce what ships, for TWO reasons rather than the one recorded. (1) 0x1600 is absent, as this note already said. (2) **Recorded nowhere before now: the upstream `.defs` is stale against its OWN upstream output.** It declares `uint16_t key, uint16_t modifiers` for KeyDown/KeyUp (0x1008/0x1009) while both `ref-BrowserServer/Src/BrowserServerBase.cpp` and ours read a third `int32 chr` — upstream hand-edited its own generated file and never updated the `.defs`, the tell being the six-space indent on the `chr` lines inside a tab-indented generated file. Regenerating from it rewrites two LIVE commands from 12 wire bytes to 4 and breaks every keystroke, which is a bigger break than losing 0x1600 and which the obvious "just add the command to the upstream .defs" fix would have shipped. **What landed:** a CORRECTED authoritative `.defs` now lives in the tree at `render/browserserver/CodeGen/BrowserYapCommandMessages.defs`, and regenerating from it was verified to reproduce the shipped `BrowserServerBase.cpp` with four COSMETIC differences only (copyright year, the parenthetical after `// SetExtraBuffer`, and the two `chr` lines re-indented from spaces to tabs) — the wire is identical. Plus `build/webos-oe/check-yap-contract.sh`, wired into `build-daemon-arm.sh`, which fails the build on any of it: a frozen id manifest (73 commands, 58 messages) diffed against both generated pairs, server-accepted cross-checked against client-sent, and the 0x1600 arm and the `chr` argument asserted specifically. Verified three ways: passes on this tree, fails with 10 named defects on a real upstream regeneration, fails with EXACTLY ONE on a regeneration from the new in-tree `.defs`. **Still `[~]` for that one, and no `.defs` edit can ever close it** *(SUPERSEDED 2026-08-15 — closed by patching the generator, exactly as the last sentence of this note prescribed; see the note above)*: YapCodeGen emits every async command as `= 0;` (`YapCodeGen.cpp:323`), so it structurally cannot produce the non-pure-virtual empty default body that makes 0x1600 optional on the server side (AC2). That single hand edit must be re-applied after every regeneration, and the guard names it. Closing this box fully means vendoring and patching the generator itself. **Already guarded at build time, so do not add a second check for it:** `JihadBrowserServer.h` uses `override` on both `asyncCmdSetExtraBuffer` and `asyncCmdKeyDown`, so losing either DECLARATION fails to compile; what compiles fine and regresses silently is the `case 0x1600:` dispatch arm, a statement invisible to the type system. **TRAP, confirmed by running it** *(FIXED 2026-08-15 in the vendored generator; the upstream one still hangs)*: a `.defs` line beginning with a SPACE hangs YapCodeGen forever — `ignoreLine()` scans for a non-space without advancing its pointer. No device result is claimed here; this is all host-side generator output and text diffs.)*
**Dependencies:** none

### R2: Shared-memory framebuffer semantics preserved
**Description:** Frames are delivered through the same double-buffered shared-memory protocol.
**Acceptance Criteria:**
- [x] `connect` and `thaw` accept two shared-buffer keys and a size and attach both segments.
- [x] The renderer fills the inactive buffer and emits exactly one paint-ready notification naming that buffer.
- [x] The renderer does not reuse a buffer until it has been returned by the client.
- [x] Pixel format, stride, and size match the upstream offscreen-buffer contract (32-bit, page dimensions).
**Dependencies:** cavekit-offscreen-rendering.md (R2, R3)

### R3: Daemon lifecycle and page manager preserved
**Description:** The daemon process behaves like the upstream render daemon.
**Acceptance Criteria:**
- [x] Daemon starts, accepts a `connect`, and creates one page per `identifier`.
- [x] `freeze`/`thaw` detach/reattach buffers; `purgePage` and low-memory purge behave as upstream.
- [x] The daemon exits after the last client disconnects (when built with that option).
- [x] Multiple pages (cards) are managed independently.
**Dependencies:** none (engine-agnostic daemon scaffolding kept from upstream; the engine instance it manages is specified in cavekit-engine-embedding.md R2)

### R4: LunaService surface preserved
**Description:** The service methods the UI calls directly remain available on the device build.
**Acceptance Criteria:**
- [x] `clearCache` and `clearCookies` are registered as a LUNA SERVICE and reach THIS daemon on the device build. *(2026-08-05 — done, under a per-variant name rather than `com.palm.browserServer`, which is the stock daemon we coexist with. Proven on device: the daemon logs `luna: serving palm://net.riverstonerelay.jihadBrowser/{clearCache,clearCookies}`, `luna-send` to that URI returns `{"returnValue":true}`, and — the decisive part, because a `true` from the STOCK browser is exactly the false pass this criterion was written around — **our own daemon logs `luna: clearCookies` and `luna: clearCache`**, i.e. the hub routed the call to us and our handlers ran. Those handlers call the same `jihad::ClearCookies`/`ClearCache` the YAP commands already use. **And the ACTION is proven on a NON-EMPTY store** (2026-08-05, second run): three real cookies seeded through the cookie SERVICE (`nsICookieService::SetCookieString`, i.e. Set-Cookie semantics — not a synthetic row), giving `cookie count=3`; then `luna-send … /clearCookies` returned true with `luna: clearCookies` in our log; then `cookie count=0`. Registration, routing and deletion are each observed separately. One practical note for re-running it: the inject channel needs a live PAGE, so the card must be launched AT a url first (`params.target`) — with no page every `cookie` command is dropped with `inject: no page`, which reads exactly like a broken cookie store. **Three things had to be right, and each was wrong first:** (1) the service NAME may not contain a hyphen — LS2 rejects it at the caller with `_UriParse: Not a valid service name`, before any daemon sees it, so `jihad-browser` became `jihadBrowser` (camelCase, matching `com.palm.browserServer`); (2) the hub needs prv+pub role files keyed on `exeName`, which is the BUNDLED LOADER `ld-2.23.so`, not `jihad-browserserver`, since the job execs through it; (3) `LSMethod` on this device has THREE fields — the TouchPad ships luna-service**2**, not the LS1 shape the in-workspace luna-sysmgr sources use — and a two-field struct makes the library walk the table at an 8-byte stride where it reads 12, so registration SUCCEEDS while registering nothing and every call answers `Unknown method "clearCookies" for category "/"`.)*
  *The clearing itself is NOT missing: `asyncCmdClearCache`/`asyncCmdClearCookies` exist over YAP and are implemented in the daemon (`jihad::ClearCache/ClearCookies`). What is missing is a route from the card to them — the isis adapter exposes no scriptable `clearCache`/`clearCookies`, which is why the app uses the Luna URI. Two ways out, and it is a CONTRACT decision rather than a bug fix: register a per-variant service (`palm://net.riverstonerelay.jihad-browser/...`) and point each app at its own — a documented divergence like the MIME/YAP rebrand — or expose the two as scriptable adapter methods, which widens the frozen app-facing call set.*

  ***DECISION, 2026-08-04: register a PER-VARIANT service. This is no longer an open choice.*** *It
  is not a coin flip once the project's own precedent is applied: the second option widens the
  `callBrowserAdapter` set, which cavekit-ui-shell.md R2 holds byte-identical to upstream and marks
  MET — so taking it would knowingly break a met criterion in another kit. The first option is the
  same move already made, deliberately, four times over for coexistence: our own NPAPI MIME, our own
  YAP service name, our own upstart job, our own state directory. A Luna service name is the fifth
  instance of one pattern, not a new kind of divergence, and it leaves the app-facing contract
  untouched. **What implementing it takes:** the daemon registers `palm://net.riverstonerelay.jihad-browser{,-mochi,-mojo}/`
  with `clearCache` + `clearCookies` routed to the existing `jihad::ClearCache/ClearCookies`, name
  derived from `JIHAD_BS_NAME` like every other per-variant identity; each app's Preferences points
  at its own URI. Note there is no `lunaservice.h` in the sysroot — the device has
  `/usr/lib/liblunaservice.so` but no headers here — so the practical route is `dlopen` + the handful
  of `LS*` entry points, which also keeps the daemon loadable if the library is ever absent.
  Add `plan-variant-identity.md`'s table as the single source for the new name.)*
- [x] On the desktop build the service layer can be compiled out without affecting the YAP path.
**Dependencies:** cavekit-browser-services.md (R2)

### R5: BrowserAdapter drives the daemon; no change for the engine swap
**Description:** The NPAPI BrowserAdapter, rebuilt, drives the Goanna daemon through a full load+paint cycle. The engine swap requires no adapter change; the only adapter edit is a self-contained **coexistence rebrand** that leaves the YAP command/message interface untouched.
**Acceptance Criteria:**
- [x] The rebuilt BrowserAdapter connects to the daemon and completes a load+paint cycle (on-device: `http://example.com` → `loaderr failed=0`, `painted bytes=723456`).
- [x] No adapter source change is required to accommodate the **Goanna backend** — the YAP command/message interface (R1) is byte-identical.
- [x] The self-contained build adds only a **two-line rebrand** — MIME `application/x-jihad-browser` (`AdapterGetMIMEDescription`) and YAP server name `BrowserClientBase("jihad-browser", …)` — so `BrowserAdapterJihad.so` coexists with the stock adapter without collision. This does not add/remove/rename/re-type any YAP command or message.
**Dependencies:** cavekit-offscreen-rendering.md, cavekit-input-bridging.md, jihad-self-contained-arch.md

## Out of Scope
- How frames are produced or how engine events originate (rendering/engine domains).
- The transport library internals (YAP/libYap is reused as-is).

## Cross-References
- See also: cavekit-offscreen-rendering.md, cavekit-engine-embedding.md, cavekit-browser-services.md, cavekit-ui-shell.md

## Changelog
- 2026-06-30: Initial draft.
- 2026-08-15: R1's last criterion closed. The YAP generator is vendored into the tree and patched, so regeneration from the in-tree `.defs` reproduces the shipped interface — including the 0x1600 empty default body — with no hand edit. `context/impl/impl-yap-codegen.md`.
- 2026-07-04: Reconciled — R1 byte-identical YAP, R2 shmem double-buffer, R3 daemon lifecycle/freeze-thaw all verified (ROUND-TRIP + FREEZE-THAW PASS). R4 device LunaService registration and R5 real NPAPI BrowserAdapter rebuild remain device-integration work (desktop compiles the service layer out).
