# Source Code — render daemon + IPC (Apache-2.0)

Engine-agnostic BrowserServer parts (YAP, shmem, daemon, LunaService). The
`BrowserServerBase` YAP interface is autogenerated — do not hand-edit, with one
documented exception (command 0x1600, the third shared buffer; see README.md and
docs/IPC-CONTRACT.md). Regenerate ONLY from CodeGen/BrowserYapCommandMessages.defs in this
dir, never from ../../../ref-BrowserServer/CodeGen, which is stale on 0x1600 AND on
KeyDown/KeyUp; then run build/webos-oe/check-yap-contract.sh, which fails the build if the
wire contract drifted. See
README.md here for keep-vs-replace.

See context/kits/ for requirements this code implements.
See context/plans/ for task dependency graphs.
