# BrowserServer source import manifest

Imported from the isis-project BrowserServer (`../../../../ref-BrowserServer/Src`,
SRCREV e2506f58, Apache-2.0) for Tasks T-004/T-005/T-006. All original Apache-2.0
headers are preserved unmodified. QtWebKit-specific files are **not** imported —
the Goanna backend in `../../goanna/` replaces them.

## Bucket 1 — engine-agnostic, imported verbatim

| File(s) | Role | Task |
|---------|------|------|
| `BrowserServerBase.{h,cpp}` | Autogenerated YAP interface (commands + messages). **Byte-identical to upstream — verified by md5. Do not hand-edit; regenerate from .yap.** | T-004 |
| `BrowserOffscreenInfo.h`, `BrowserOffscreenCalculations.h` | Shared framebuffer pixel-format + scroll/zoom calc contract | T-005 |
| `IpcBuffer.{h,cpp}`, `BrowserSyncReplyPipe.{h,cpp}` | YAP IPC transport + sync-reply pipe (dialogs) | T-005 |
| `BrowserRect.{h,cpp}` | Geometry helper | T-005 |
| `BrowserCommon.h`, `BrowserAdapterTypes.h`, `CmdResourceHandlers.h`, `DbBackupStatus.h`, `SSLValidationInfo.h` | Shared types/enums (incl. EditorState, MouseMode, SSL info) | T-006 |
| `CpuAffinity.{h,cpp}`, `PluginDirWatcher.{h,cpp}`, `JsonUtils.{h,cpp}`, `SSLSupport.{h,cpp}`, `BackupManager.{h,cpp}` | Engine-agnostic daemon support | T-006 |

## Bucket 2 — imported, needs adaptation before it builds

| File(s) | Adaptation needed |
|---------|-------------------|
| `BrowserServer.{h,cpp}` | De-Qt: the daemon currently constructs/manages QtWebKit `BrowserPage`. Rebind to the Goanna-backed `BrowserPage` and drop QtWebKit init. |
| `Main.cpp` | Drop `QApplication`/Qt event loop; use the engine event-loop integration (T-019). |
| `BrowserPageManager.{h,cpp}` | Includes `BrowserPage.h` (21 refs). Engine-agnostic in role; needs the `BrowserPage` type provided by the Goanna backend (see boundary note). |
| `Settings.{h,cpp}` | **Engine-coupled, not a mechanical de-Qt.** Beyond `QString`, it drives the QtWebKit settings system (`webOS::WebSettings::initSettings`, `weboswebsettings.h`) and `QSettings`. For Goanna these map to `nsIPrefBranch`/profile prefs entirely differently — reimplement as part of the engine-settings work (cavekit-browser-services R1 / T-023), not a port. The path/quota defaults here are still a useful reference. |

## Bucket 3 — NOT imported (Goanna backend replaces)

`BrowserPage.{h,cpp}`, `BrowserOffscreenQt.{h,cpp}`, `WebKitEventListener.{h,cpp}`,
`BrowserComboBox.{h,cpp}`. Reference copies remain in `../../../../ref-BrowserServer/Src`.

## Integration boundary

`BrowserServerBase` dispatches every YAP command to a `BrowserPage*`. Keeping that
vtable identical is what lets the engine swap stay invisible to the UI/adapter.
The Goanna backend supplies a `BrowserPage` (see `../../goanna/BrowserPageGoanna.h`,
to be aliased/adapted to the `BrowserPage` name the manager and dispatch expect).
Until that header is provided, `BrowserPageManager` and `BrowserServer` will not
compile — this is the intended Phase-1 integration point, tracked in the build site.
